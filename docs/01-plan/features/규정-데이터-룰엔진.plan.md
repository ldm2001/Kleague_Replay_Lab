# 1단계 규정 데이터와 Rule Engine 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.
>
> **이 계획에는 커밋 단계가 없음.** 사용자 결정으로 이번 작업에서는 Git을 건드리지 않음. `git init`·`git add`·`git commit`을 실행하지 말 것. `.git.disabled`는 그대로 둠.

**Goal:** 밀기 1개 유형 × IFAB 2개 판본으로 규정 데이터 → 규칙 인터프리터 → 근거 조항까지 이어지는 수직 관통을 뚫고, 합성 픽스처 27건과 불변식 4건을 통과시킨다.

**Architecture:** 판본별 규칙은 `packages/rule-data`의 JSON에 있고 엔진은 그것을 해석하는 결정론적 인터프리터다. 엔진과 데이터 사이에는 직접 의존이 없고 `packages/shared-types`가 정의한 `RuleSet` 인터페이스만 오간다. 엔진은 판정 결론이 아니라 **어느 조항이 무엇을 요구하고 어디서 막혔는지**를 반환하며, VAR 판단은 밀기 판정과 독립된 경로에서 네 게이트를 각각 별도 필드로 계산한다.

**Tech Stack:** TypeScript 5 (strict), Node 23 + npm workspaces, Vitest, JSON 규칙 데이터. DB·네트워크·LLM 없음. 1단계 산출물은 전부 순수 함수다.

## Global Constraints

이 절은 모든 태스크의 요구사항에 암묵적으로 포함된다.

- **심판 개인 식별 정보를 어떤 형태로도 저장하거나 표시하지 않음.** 픽스처·타입·데이터 어디에도 심판 이름, 심판 ID, 경기 ID, 팀명, 선수명, 날짜를 넣지 않는다. `fact_signature`는 경기·심판 식별자를 포함하지 않는다 (var-20이 검사).
- **규정 원문 전문을 저장하거나 서빙하지 않음.** 조항 단위 인용(`quoteSnapshot`)과 출처(`sourcePage`)만 보관한다. 한 인용의 `quoteSnapshot`은 해당 조항의 발췌를 넘지 않는다.
- **`rules/` 아래 PDF·HTML은 저장소에 넣지 않음.** 이 계획의 인용문은 로컬 PDF에서 발췌해 계획서 안에 옮겨 적은 것이므로 구현자는 PDF를 다시 열 필요가 없다.
- **영상 파일은 어떤 형태로도 저장소에 두지 않음.** 1단계에는 영상이 등장하지 않는다.
- **VAR 네 게이트(범주·시한·문턱·절차)를 하나의 값으로 합치지 않음.** `reviewable: true` + `intervention: "NO_INTERVENTION"`이 동시에 표현되어야 한다.
- **엔진 소스에 판본 문자열 리터럴이 없어야 함.** `packages/rule-engine/src` 안에 `2025-26`·`2026-27`·`2025/26`·`2026/27` 문자열이 나타나면 1단계 실패다 (Task 12의 검사기가 강제).
- **모르는 값을 채우지 않음.** 대회 채택 옵션이 없으면 `UNKNOWN_COMPETITION_OPTION`을 반환하고 미채택으로 접지 않는다. 확인되지 않은 사실은 `UNMET`이며 추정값을 넣지 않는다.
- **조항 없이 결론만 반환하는 경로를 만들지 않음.** 모든 `EvaluationResult`의 `citations`는 최소 1개다 (push-09가 검사).
- 모든 패키지는 TypeScript `strict: true`, `noUncheckedIndexedAccess: true`.
- 주석과 문서 문자열은 한국어. 열거값과 식별자는 영어 대문자 스네이크.

## README와의 의도적 차이 다섯 가지

README 11절의 코드는 "인터프리터가 다뤄야 할 판단 구조를 보여주는 예시"이고 8절은 저장 계약이다. 둘을 동시에 만족시키기 위해 다음 다섯 가지를 확장한다. 이건 임의 변경이 아니라 README 안의 두 절을 맞추는 작업이다.

1. **`VarAssessment`에 `notReviewableReason` / `windowClosedReason` / `windowException`을 추가한다.** README 8절 `decision_results`가 이 세 열을 요구한다. 11절 타입에만 없다. "어느 게이트가 막았는지"와 "그 게이트가 왜 막았는지"는 다른 질문이라는 8절 문장이 근거다.
2. **`EvaluationResult`에 `factSignature`와 `factSignatureInput`을 추가한다.** 8절이 `fact_signature` 열을 요구하고 var-20이 그 값을 검사한다. `factSignatureInput`은 해시의 원문(정규화 문자열)이며, var-20이 그 안에 식별자가 없음을 확인하는 데 쓴다.
3. **오류는 판정 결과가 아니라 별도 반환 형태로 낸다.** 13절 var-21이 `error: UNKNOWN_COMPETITION_OPTION`을 기대한다. `VarOutcome` 판별 유니온으로 성공과 오류를 나눈다. 오류를 `Decision`에 섞으면 "규정이 없다"(`OUT_OF_SCOPE`)와 "우리가 모른다"(오류)가 한 값이 된다.
4. **`evaluatePush`는 게이트보다 먼저 `accounts`를 만든다.** README 11절 스케치는 각도 게이트에서 바로 `inconclusive(...)`를 반환하는데, 그러면 `accounts`가 빈 채로 나가 push-10 불변식("사실값이 막혀도 `accounts`가 비지 않는가")이 깨진다. 11절 본문 자체가 "`accounts`와 `conflicts`를 `decision`보다 먼저 반환함"이라고 적고 있으므로 본문 쪽을 따른다. 게이트는 `decision` 계열 필드만 정한다.
5. **`VAR_WINDOW_EXCEPTIONS`를 3값에서 5값으로 넓힌다.** README 8절(1204행)은 `MISTAKEN_IDENTITY | VIOLENT_CONDUCT | NONE`을 적었는데, 이 세 값으로는 "침 뱉기로 재개 후 퇴장"을 표현할 수 없다. 원문(IFAB VAR 1.10 / Law 5.3)이 나눈 예외는 선수 확인 오류와 **세 가지** 퇴장 사안(폭력 행위·침 뱉기 또는 물기·심한 모욕)이다. 원문이 나눈 만큼으로 넓혀 `SEND_OFF_CATEGORIES`와 값을 맞췄고, 그래서 예외 판정에 변환표가 없다. 이 확장은 Task 8의 매핑 결정 2번에서 다시 다룬다.

## 1단계에서 하지 않는 것

- KFA 한국어 대조판과 K리그 대회요강 조항은 데이터에 넣지 않는다. 14절 1단계는 "Law 12와 VAR 프로토콜만 수동 발췌"라고 적고 있고, KFA 대조판 확보는 15절에서 아직 열린 항목이다. 따라서 1단계의 `accounts`는 IFAB 한 층이고 `conflicts`는 항상 빈 배열이다. **계층 충돌 메커니즘은 타입과 데이터 필드로 존재하되 데이터가 확인될 때까지 채우지 않는다.** 없는 충돌을 지어내면 3절의 3계층 구조를 검증하는 게 아니라 흉내내는 것이 된다.
- `intervention: "CONFIRMED"`로 가는 경로는 만들지 않는다. `CONFIRMED`는 실제 리뷰가 일어나고 원심이 유지된 관측값인데 1단계에는 관측 입력이 없다. 도달 불가 상태를 억지로 만들지 않고, 열거값만 정의해 둔다.
- DB·마이그레이션·API·화면은 2단계다. 1단계 산출물은 순수 함수와 JSON뿐이다.

---

## 파일 구조

```text
Replay_Lab/
├── package.json                      # npm workspaces 루트 · 테스트/검사 스크립트
├── tsconfig.base.json                # 공통 컴파일러 옵션 + paths 별칭
├── vitest.config.ts                  # 워크스페이스 전체 테스트 러너
│
├── packages/shared-types/
│   ├── package.json
│   ├── tsconfig.json
│   ├── src/
│   │   ├── vocabulary.ts             # 판본과 무관한 어휘(열거값)의 SSOT
│   │   ├── citation.ts               # RuleCitation · AuthorityAccount · FactRequirement · LayerConflict
│   │   ├── facts.ts                  # Observed<T> · PushFacts · VarFacts · CompetitionOptions
│   │   ├── evaluation.ts             # VarAssessment · EvaluationResult · VarOutcome
│   │   ├── rule-set.ts               # RuleSet 인터페이스 (엔진이 요구하고 데이터가 구현)
│   │   └── index.ts
│   └── test/
│       ├── vocabulary.test.ts        # 어휘 배열의 값·순서·동결
│       └── types.test.ts             # 타입 계약의 컴파일 가능성
│
├── packages/rule-data/
│   ├── package.json
│   ├── tsconfig.json
│   ├── data/
│   │   ├── ifab/
│   │   │   ├── 2025-26.json          # Law 12 + VAR 프로토콜 수동 발췌 · 4개 범주
│   │   │   └── 2026-27.json          # 같은 구조 · 5개 범주 + 코너킥 대회 옵션
│   │   ├── kleague/                  # 3층 권한 · 2단계
│   │   └── competition-options/      # 판본과 별개 축 · 2단계
│   ├── schemas/                      # 2단계 · JSON 스키마
│   ├── src/
│   │   ├── schema.ts                 # JSON 파일의 형태 타입
│   │   ├── load-rule-set.ts          # loadRuleSet(versionId) → RuleSet | null
│   │   └── index.ts
│   └── test/
│       └── load-rule-set.test.ts     # 알 수 없는 판본에 null · 인용 해시
│
├── packages/rule-engine/
│   ├── package.json
│   ├── tsconfig.json
│   ├── fixtures/
│   │   ├── push-decision.fixtures.json   # 케이스 8 + 불변식 2
│   │   └── var-assessment.fixtures.json  # 케이스 19 + 불변식 2
│   ├── src/
│   │   ├── interpreter/
│   │   │   └── build-accounts.ts     # 게이트보다 먼저 실행 · accounts/blockedFrom/narrowedTo
│   │   ├── evaluators/
│   │   │   ├── pushing/evaluate-push.ts  # 밀기 판정 조립
│   │   │   ├── charging/             # 2단계
│   │   │   └── handball/             # 2단계
│   │   ├── var/
│   │   │   └── evaluate-var.ts       # VAR 네 게이트 · 반칙 평가와 별개 축
│   │   ├── gates/
│   │   │   └── push-gates.ts         # 각도 게이트 · 속도 게이트
│   │   ├── specifications/           # 2단계 · 명세 조합
│   │   ├── signatures/
│   │   │   └── fact-signature.ts     # 정규화 해시
│   │   └── index.ts
│   └── test/
│       ├── build-accounts.test.ts
│       ├── gates.test.ts
│       ├── evaluate-push.test.ts
│       ├── evaluate-var.test.ts
│       ├── fact-signature.test.ts
│       └── fixtures.test.ts          # 27 케이스 + 4 불변식 러너
│
├── scripts/check/
│   ├── no-edition-literal.mjs        # 엔진 소스의 판본 리터럴 금지
│   └── enums.mjs                     # 픽스처의 값이 vocabulary에 있는지
│
└── rules/                            # 이미 존재 · 원문 PDF는 여기 그대로 두고 옮기지 않는다
    ├── ifab/{2024-25,2025-26,2026-27}/metadata.json   # 미작성 (README 14절 1단계 항목 1)
    └── kleague/{2025,2026}/{kleague1,kleague2}/metadata.json
```

`rules/` 아래의 PDF·HTML은 저장소에 포함하지 않는다 (README 2절). 이 계획이 만드는 것은 `metadata.json`뿐이고 원문은 손대지 않는다.

의존 방향은 한 방향뿐이다.

```text
rule-engine ─→ shared-types ←─ rule-data
```

`rule-engine`은 `rule-data`를 import하지 않는다. 판본 선택은 호출자(1단계에서는 픽스처 러너, 2단계부터는 use-case)가 한다. 이 경계가 "엔진에 판본 문자열이 없다"를 구조로 보장한다.

---

### Task 1: 워크스페이스 골격과 어휘 SSOT

**Files:**
- Create: `package.json`, `tsconfig.base.json`, `vitest.config.ts`
- Create: `packages/shared-types/package.json`, `packages/shared-types/tsconfig.json`
- Create: `packages/shared-types/src/vocabulary.ts`
- Test: `packages/shared-types/test/vocabulary.test.ts`

**Interfaces:**
- Consumes: 없음 (첫 태스크)
- Produces: 상수 배열 `AUTHORITIES` `DECISIONS` `SEVERITIES` `OBSERVATION_SPEEDS` `RELEVANCES` `FACT_STATUSES` `FACT_BLOCKERS` `CAMERA_SUFFICIENCY_LEVELS` `RESTART_TYPES` `DISCIPLINARY_ACTIONS` `DECISION_MATCHES` `CONFIDENCE_LEVELS` `INCONCLUSIVE_REASONS` `VAR_CATEGORIES` `VAR_THRESHOLD_RESULTS` `VAR_INTERVENTIONS` `VAR_NO_INTERVENTION_REASONS` `VAR_NOT_REVIEWABLE_REASONS` `VAR_WINDOW_CLOSED_REASONS` `VAR_WINDOW_EXCEPTIONS` `VAR_REVIEW_PROCEDURES` `REVIEW_SCENARIOS` `SEND_OFF_CATEGORIES` `DECISION_NATURES` `ERROR_MAGNITUDES` `EVALUATION_ERROR_CODES`, 그리고 각각의 동명 단수 타입(`Authority`, `Decision`, …). 별칭 `@replay/shared-types`로 import.

- [x] **Step 1: 루트 워크스페이스 파일 세 개를 만든다**

`package.json`:

```json
{
  "name": "replay-lab",
  "private": true,
  "type": "module",
  "workspaces": ["packages/*"],
  "engines": { "node": ">=22" },
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.base.json --noEmit",
    "check:enums": "node scripts/check/enums.mjs",
    "check:edition": "node scripts/check/no-edition-literal.mjs",
    "check": "npm run typecheck && npm run check:edition && npm run check:enums && npm run test"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
```

`tsconfig.base.json`:

```json
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "lib": ["ES2023"],
    "types": ["node"],
    "strict": true,
    "noUncheckedIndexedAccess": true,
    "exactOptionalPropertyTypes": true,
    "noImplicitOverride": true,
    "noEmit": true,
    "skipLibCheck": true,
    "resolveJsonModule": true,
    "verbatimModuleSyntax": true,
    "baseUrl": ".",
    "paths": {
      "@replay/shared-types": ["packages/shared-types/src/index.ts"],
      "@replay/rule-data": ["packages/rule-data/src/index.ts"],
      "@replay/rule-engine": ["packages/rule-engine/src/index.ts"]
    }
  },
  "include": ["packages/*/src/**/*.ts", "packages/*/test/**/*.ts", "vitest.config.ts"]
}
```

`vitest.config.ts`:

```ts
import { fileURLToPath } from "node:url";
import { defineConfig } from "vitest/config";

const resolvePackage = (relativePath: string) =>
  fileURLToPath(new URL(relativePath, import.meta.url));

export default defineConfig({
  resolve: {
    alias: {
      "@replay/shared-types": resolvePackage("./packages/shared-types/src/index.ts"),
      "@replay/rule-data": resolvePackage("./packages/rule-data/src/index.ts"),
      "@replay/rule-engine": resolvePackage("./packages/rule-engine/src/index.ts"),
    },
  },
  test: {
    include: ["packages/*/test/**/*.test.ts"],
  },
});
```

TypeScript 프로젝트 레퍼런스를 쓰지 않는 이유: 1단계 산출물은 빌드 결과가 아니라 테스트 통과다. `paths` 별칭이면 빌드 순서를 관리할 필요가 없다.

- [x] **Step 2: shared-types 패키지 파일을 만든다**

`packages/shared-types/package.json`:

```json
{
  "name": "@replay/shared-types",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts"
}
```

`packages/shared-types/tsconfig.json`:

```json
{
  "extends": "../../tsconfig.base.json",
  "include": ["src/**/*.ts", "test/**/*.ts"]
}
```

- [x] **Step 3: 실패하는 테스트를 쓴다**

`packages/shared-types/test/vocabulary.test.ts`:

```ts
import { describe, expect, it } from "vitest";
import * as vocabulary from "../src/vocabulary.js";

// `vocabulary()`가 리터럴 튜플 타입을 돌려주므로 `Object.entries`의 값 자리는
// 어휘별 튜플의 합집합이 된다. `unknown`으로 한 번 넓혀야 서술어가 성립한다.
const vocabularyArrays = (): [string, readonly string[]][] =>
  (Object.entries(vocabulary) as [string, unknown][]).filter(
    (entry): entry is [string, readonly string[]] => Array.isArray(entry[1]),
  );

describe("vocabulary", () => {
  it("VAR 범주는 README 8절의 여섯 값을 그 순서로 갖는다", () => {
    expect(vocabulary.VAR_CATEGORIES).toEqual([
      "GOAL_NO_GOAL",
      "PENALTY_NO_PENALTY",
      "RED_CARD",
      "MISTAKEN_IDENTITY",
      "CORNER_KICK",
      "NONE",
    ]);
  });

  it("검토 불가 사유와 창 종료 사유를 하나의 열거로 합치지 않는다", () => {
    expect(vocabulary.VAR_NOT_REVIEWABLE_REASONS).toEqual([
      "OUTSIDE_REVIEWABLE_CATEGORIES",
      "COMPETITION_OPTION_NOT_ADOPTED",
    ]);
    expect(vocabulary.VAR_WINDOW_CLOSED_REASONS).toEqual(["PLAY_RESTARTED"]);
    for (const reason of vocabulary.VAR_NOT_REVIEWABLE_REASONS) {
      expect(vocabulary.VAR_WINDOW_CLOSED_REASONS).not.toContain(reason);
    }
  });

  it("모든 어휘 배열은 중복이 없고 동결되어 있다", () => {
    const arrays = vocabularyArrays();
    expect(arrays.length).toBeGreaterThan(20);
    for (const [name, values] of arrays) {
      expect(new Set(values).size, `${name}에 중복 값이 있다`).toBe(values.length);
      expect(Object.isFrozen(values), `${name}이 동결되지 않았다`).toBe(true);
    }
  });

  it("판본 문자열은 어휘에 들어가지 않는다", () => {
    const flat = vocabularyArrays().flatMap(([, values]) => values);
    expect(flat.some((value) => /\d{4}[-/]\d{2}/.test(value))).toBe(false);
  });
});
```

마지막 케이스가 중요하다. 어휘는 판본과 무관한 것만 담는다. 판본에 따라 달라지는 것(어떤 범주가 열려 있는가)은 `rule-data`가 소유한다.

- [x] **Step 4: 실패를 확인한다**

Run: `npm install && npx vitest run packages/shared-types`
Expected: FAIL — `Cannot find module '../src/vocabulary.js'`

- [x] **Step 5: `vocabulary.ts`를 쓴다**

`packages/shared-types/src/vocabulary.ts`:

```ts
/**
 * 판본과 무관한 어휘의 단일 소유자.
 *
 * 여기 있는 것: 값의 종류 (어떤 판정값이 존재하는가)
 * 여기 없는 것: 어느 판본에서 무엇이 열려 있는가 → packages/rule-data가 소유
 *
 * 이 구분을 지키지 않으면 판본이 바뀔 때 타입을 고치게 되고,
 * 규정 개정이 코드 배포와 묶인다 (README 11절).
 */

const vocabulary = <const T extends readonly string[]>(values: T): Readonly<T> =>
  Object.freeze(values);

export const AUTHORITIES = vocabulary(["IFAB", "KFA", "KLEAGUE"]);
export type Authority = (typeof AUTHORITIES)[number];

export const DECISIONS = vocabulary([
  "FOUL",
  "NO_FOUL",
  "NORMAL_CONTACT",
  "INCONCLUSIVE", // 영상 근거 부족 — 더 나은 영상이 있으면 답이 나온다
  "OUT_OF_SCOPE", // 규정에 조문 없음 — 영상을 아무리 봐도 답이 없다
]);
export type Decision = (typeof DECISIONS)[number];

export const SEVERITIES = vocabulary(["CARELESS", "RECKLESS", "EXCESSIVE_FORCE"]);
export type Severity = (typeof SEVERITIES)[number];

/**
 * 사실값으로 들어오는 강도. "uncertain"은 정도가 아니라 관측 결과의 부재이므로
 * SEVERITIES(규정이 구분하는 세 단계)와 섞지 않는다.
 */
export const OBSERVED_SEVERITIES = vocabulary([...SEVERITIES, "uncertain"]);
export type ObservedSeverity = (typeof OBSERVED_SEVERITIES)[number];

/** "none"은 밀림이 없었다는 관측이고 "uncertain"은 관측하지 못했다는 뜻이다. */
export const DISPLACEMENT_LEVELS = vocabulary(["none", "possible", "clear", "uncertain"]);
export type DisplacementLevel = (typeof DISPLACEMENT_LEVELS)[number];

export const OBSERVATION_SPEEDS = vocabulary(["NORMAL", "SLOW", "UNKNOWN"]);
export type ObservationSpeed = (typeof OBSERVATION_SPEEDS)[number];

export const RELEVANCES = vocabulary(["PRIMARY", "SUPPORTING", "COUNTER_READING"]);
export type Relevance = (typeof RELEVANCES)[number];

export const FACT_STATUSES = vocabulary(["ESTABLISHED", "UNMET"]);
export type FactStatus = (typeof FACT_STATUSES)[number];

/** `null`은 "관측했으나 값을 특정하지 못함"이고, 여기 값들은 "관측 자체가 막힘"이다. */
export const FACT_BLOCKERS = vocabulary(["CAMERA", "SPEED", "NOT_IN_FOOTAGE"]);
export type FactBlocker = (typeof FACT_BLOCKERS)[number];

export const CAMERA_SUFFICIENCY_LEVELS = vocabulary(["LOW", "MEDIUM", "HIGH"]);
export type CameraSufficiency = (typeof CAMERA_SUFFICIENCY_LEVELS)[number];

/**
 * 관측 재개와 추천 재개가 같은 어휘를 쓴다 (README 8절 `observed_restart_type`).
 * 두 벌을 두면 `decision_match` 비교가 값 변환을 먼저 해야 하고,
 * 변환표가 곧 두 어휘가 어긋날 수 있는 자리가 된다.
 *
 * 엔진은 이 중 일부만 낸다 — `UNKNOWN`은 관측 쪽 전용이고,
 * 엔진이 재개를 특정하지 못하면 `restart: null`에 INCONCLUSIVE를 붙인다.
 */
export const RESTART_TYPES = vocabulary([
  "DIRECT_FREE_KICK",
  "INDIRECT_FREE_KICK",
  "FREE_KICK_UNSPECIFIED",
  "PENALTY_KICK",
  "DROP_BALL",
  "THROW_IN",
  "GOAL_KICK",
  "CORNER_KICK",
  "KICK_OFF",
  "PLAY_CONTINUED", // 재개 없이 속행. "NONE"이 아니다 (README 8절 1250행)
  "UNKNOWN",
]);
export type RestartType = (typeof RESTART_TYPES)[number];

export const GOAL_DECISIONS = vocabulary(["GOAL", "NO_GOAL", "NOT_APPLICABLE", "UNKNOWN"]);
export type GoalDecision = (typeof GOAL_DECISIONS)[number];

/**
 * `observed_card`와 `disciplinary_action`이 함께 쓰는 어휘.
 *
 * 엔진은 `SECOND_CAUTION`을 내지 않는다 — 이 사건 하나만 보고는
 * 해당 선수가 이미 경고를 받았는지 알 수 없기 때문이다. 관측 쪽에서만 들어온다.
 */
export const DISCIPLINARY_ACTIONS = vocabulary([
  "NONE",
  "CAUTION",
  "SECOND_CAUTION",
  "SEND_OFF",
]);
export type DisciplinaryAction = (typeof DISCIPLINARY_ACTIONS)[number];

export const DECISION_MATCHES = vocabulary(["MATCH", "MISMATCH", "UNDETERMINED"]);
export type DecisionMatch = (typeof DECISION_MATCHES)[number];

export const CONFIDENCE_LEVELS = vocabulary(["LOW", "MEDIUM", "HIGH"]);
export type ConfidenceLevel = (typeof CONFIDENCE_LEVELS)[number];

export const INCONCLUSIVE_REASONS = vocabulary([
  "CAMERA_INSUFFICIENT", // 각도 부족
  "SEVERITY_UNDETERMINED", // 강도 특정 불가
  "SLOW_MOTION_ONLY", // 정상 속도 관측 없음 (속도 게이트)
  "OUT_OF_SCOPE", // 경기규칙에 해당 조문 없음
]);
export type InconclusiveReason = (typeof INCONCLUSIVE_REASONS)[number];

export const VAR_CATEGORIES = vocabulary([
  "GOAL_NO_GOAL",
  "PENALTY_NO_PENALTY",
  "RED_CARD",
  "MISTAKEN_IDENTITY",
  "CORNER_KICK",
  "NONE",
]);
export type VarCategory = (typeof VAR_CATEGORIES)[number];

export const VAR_THRESHOLD_RESULTS = vocabulary(["MET", "NOT_MET", "UNDETERMINED"]);
export type VarThresholdResult = (typeof VAR_THRESHOLD_RESULTS)[number];

export const VAR_INTERVENTIONS = vocabulary(["NO_INTERVENTION", "OVERTURNED", "CONFIRMED"]);
export type VarIntervention = (typeof VAR_INTERVENTIONS)[number];

/** 어느 게이트가 막았는가. */
export const VAR_NO_INTERVENTION_REASONS = vocabulary([
  "NOT_REVIEWABLE",
  "TOO_LATE",
  "THRESHOLD_NOT_MET",
]);
export type VarNoInterventionReason = (typeof VAR_NO_INTERVENTION_REASONS)[number];

/** 범주 게이트가 왜 막았는가. "규정에 없다"와 "우리 대회는 안 쓴다"는 다른 문장이다. */
export const VAR_NOT_REVIEWABLE_REASONS = vocabulary([
  "OUTSIDE_REVIEWABLE_CATEGORIES",
  "COMPETITION_OPTION_NOT_ADOPTED",
]);
export type VarNotReviewableReason = (typeof VAR_NOT_REVIEWABLE_REASONS)[number];

/** 시한 게이트가 왜 막았는가. 검토 창을 닫는 유일한 조건은 재개다. */
export const VAR_WINDOW_CLOSED_REASONS = vocabulary(["PLAY_RESTARTED"]);
export type VarWindowClosedReason = (typeof VAR_WINDOW_CLOSED_REASONS)[number];

/**
 * 재개 뒤에도 검토 창이 열려 있는 사안.
 *
 * IFAB VAR 프로토콜 1.10 / Law 5.3 원문이 다섯을 든다 —
 * "mistaken identity or ... violent conduct, spitting, biting or
 *  extremely offensive, insulting and/or abusive action(s)".
 * README 8절 초안은 앞의 둘만 적었으나 그 어휘로는
 * "침 뱉기로 재개 후 퇴장"을 표현할 수 없다.
 */
export const VAR_WINDOW_EXCEPTIONS = vocabulary([
  "MISTAKEN_IDENTITY",
  "VIOLENT_CONDUCT",
  "BITING_OR_SPITTING",
  "OFFENSIVE_LANGUAGE_OR_ACTION",
  "NONE",
]);
export type VarWindowException = (typeof VAR_WINDOW_EXCEPTIONS)[number];

export const VAR_REVIEW_PROCEDURES = vocabulary(["OFR", "VAR_ONLY", "NONE"]);
export type VarReviewProcedure = (typeof VAR_REVIEW_PROCEDURES)[number];

/**
 * 중계에서 **관측한** 판정 상황. VAR 범주가 아니다 —
 * 어느 범주에 걸리는지는 판본 데이터의 매핑이 정한다 (README 8절 1039행).
 *
 * `OTHER`는 버리는 값이 아니다. 스로인·오프사이드 단독 장면처럼
 * 어느 범주에도 닿지 않는 입력이 여기로 들어오고, 엔진은 `OUT_OF_SCOPE`를 낸다.
 * 이 값을 빼면 "규정이 다루지 않는다"고 답할 경로가 사라진다.
 */
export const REVIEW_SCENARIOS = vocabulary([
  "GOAL_DISALLOWED",
  "GOAL_AWARDED",
  "PENALTY_NOT_GIVEN",
  "PENALTY_GIVEN",
  "SENDING_OFF_NOT_GIVEN",
  "CARD_SHOWN",
  "SECOND_CAUTION",
  "CORNER_KICK_AWARDED",
  "OTHER",
]);
export type ReviewScenario = (typeof REVIEW_SCENARIOS)[number];

/**
 * IFAB Law 12.3 「Sending-off offences」 (2025/26 p.117) 아홉 항목을
 * 판정에 필요한 만큼 묶은 것.
 *
 * - DOGSO 세 항목(고의 핸드볼 · 자기 진영 밖 비고의 핸드볼 · 프리킥 반칙)은
 *   퇴장 사유로는 하나다. 셋의 구분은 재개 종류를 가를 뿐 창 예외에 영향이 없다.
 * - `BITING_OR_SPITTING`은 원문이 한 항목으로 적은 것을 그대로 둔 것이다.
 *   나눌 근거가 원문에 없다.
 */
export const SEND_OFF_CATEGORIES = vocabulary([
  "DOGSO",
  "SERIOUS_FOUL_PLAY",
  "BITING_OR_SPITTING",
  "VIOLENT_CONDUCT",
  "OFFENSIVE_LANGUAGE_OR_ACTION",
  "SECOND_CAUTION",
  "ENTERING_VOR",
  "NONE", // 퇴장 사안이 아님
]);
export type SendOffCategory = (typeof SEND_OFF_CATEGORIES)[number];

/** 절차 게이트의 입력. 문턱과 무관하며 절차만 가른다 (README 11절). */
export const DECISION_NATURES = vocabulary(["SUBJECTIVE", "FACTUAL"]);
export type DecisionNature = (typeof DECISION_NATURES)[number];

/**
 * 문턱 게이트의 입력 — 오심의 **정도**만 담는다 (README 8절 1048행).
 *
 * "심각한 미인지 사건"은 여기 값이 아니다. 원문이
 * "clear and obvious error **or** serious missed incident"로 둘을 나란히 놓으므로
 * 정도 축에 섞으면 "놓친 사건"이 "덜 명백한 오심"으로 읽힌다.
 * 미인지 여부는 `VarFacts.seriousMissedIncident` 불리언으로 따로 들어오고,
 * 문턱 게이트가 두 입력을 OR로 읽는다.
 */
export const ERROR_MAGNITUDES = vocabulary([
  "CLEAR_AND_OBVIOUS",
  "NOT_CLEAR_AND_OBVIOUS",
  "UNDETERMINED",
]);
export type ErrorMagnitude = (typeof ERROR_MAGNITUDES)[number];

/** 판정 결과가 아니라 입력이 부족하다는 신호. Decision과 섞지 않는다. */
export const EVALUATION_ERROR_CODES = vocabulary([
  "UNKNOWN_RULE_VERSION",
  "UNKNOWN_COMPETITION_OPTION",
]);
export type EvaluationErrorCode = (typeof EVALUATION_ERROR_CODES)[number];
```

- [x] **Step 6: 통과를 확인한다**

Run: `npx vitest run packages/shared-types`
Expected: PASS — 4 케이스

- [x] **Step 7: 타입 검사를 돌린다**

Run: `npm run typecheck`
Expected: 오류 없음

---

### Task 2: 인용·사실·결과 타입과 `RuleSet` 인터페이스

**Files:**
- Create: `packages/shared-types/src/citation.ts`
- Create: `packages/shared-types/src/facts.ts`
- Create: `packages/shared-types/src/evaluation.ts`
- Create: `packages/shared-types/src/rule-set.ts`
- Create: `packages/shared-types/src/index.ts`
- Test: `packages/shared-types/test/types.test.ts`

**Interfaces:**
- Consumes: Task 1의 어휘 타입 전부.
- Produces:
  - `RuleCitation` `AuthorityAccount` `FactRequirement` `LayerConflict`
  - `Observed<T>` `PushFacts` `VarFacts` `CompetitionOptions`
  - `VarAssessment` `EvaluationResult` `VarOutcome` `EvaluationFailure`
  - `RuleSet` — 메서드 `cite(conceptKey: ConceptKey): RuleCitation[]`, `varCategories(): readonly VarCategoryRule[]`, `timeWindowExceptions(): TimeWindowExceptions`, `layerConflicts(): readonly LayerConflict[]`
  - `ConceptKey` = `"LAW_12_DIRECT_FREE_KICK" | "LAW_12_DISCIPLINE" | "VAR_REVIEWABLE_CATEGORIES" | "VAR_TIME_WINDOW" | "VAR_THRESHOLD" | "VAR_REVIEW_PROCESS"`
  - `VarCategoryRule` = `{ id: VarCategory; appliesTo: readonly ReviewScenario[]; requiresCompetitionOption: string | null; requiresMistakenIdentity: boolean }`
  - `TimeWindowExceptions` = `{ mistakenIdentity: boolean; sendOffCategories: readonly SendOffCategory[] }`

- [x] **Step 1: 실패하는 테스트를 쓴다**

`packages/shared-types/test/types.test.ts`:

```ts
import { describe, expect, expectTypeOf, it } from "vitest";
import type {
  EvaluationResult,
  Observed,
  RuleCitation,
  VarAssessment,
  VarOutcome,
} from "../src/index.js";
import { isEvaluationFailure, observed } from "../src/index.js";

const citation: RuleCitation = {
  ruleId: "ifab-2025-26-law-12-1",
  ruleRevision: 1,
  ruleContentSha256: "0".repeat(64),
  authority: "IFAB",
  edition: "2025-26",
  law: "12",
  section: "1",
  relevance: "PRIMARY",
  quoteSnapshot: "A direct free kick is awarded ...",
  sourcePage: "109",
};

describe("타입 계약", () => {
  it("Observed는 값과 관측 조건을 함께 갖는다", () => {
    const fact: Observed<boolean> = observed(true, "NORMAL", ["shot-1"]);
    expect(fact).toEqual({ value: true, observedAtSpeed: "NORMAL", shotIds: ["shot-1"] });
  });

  it("VarAssessment의 네 게이트는 서로 다른 필드다", () => {
    expectTypeOf<VarAssessment>().toHaveProperty("category");
    expectTypeOf<VarAssessment>().toHaveProperty("withinTimeWindow");
    expectTypeOf<VarAssessment>().toHaveProperty("thresholdMet");
    expectTypeOf<VarAssessment>().toHaveProperty("reviewProcedure");
  });

  it("검토 대상이면서 개입하지 않은 상태를 표현할 수 있다", () => {
    const assessment: VarAssessment = {
      reviewable: true,
      category: "GOAL_NO_GOAL",
      withinTimeWindow: true,
      thresholdMet: "NOT_MET",
      intervention: "NO_INTERVENTION",
      noInterventionReason: "THRESHOLD_NOT_MET",
      notReviewableReason: null,
      windowClosedReason: null,
      windowException: "NONE",
      reviewProcedure: "OFR",
      explanation: "검토 대상에는 해당하나 명백하고 분명한 오류로 보기 어려움",
    };
    expect(assessment.reviewable).toBe(true);
    expect(assessment.intervention).toBe("NO_INTERVENTION");
  });

  it("citations는 비어 있을 수 없다는 계약을 런타임 헬퍼로 확인한다", () => {
    const result: EvaluationResult = {
      accounts: [],
      conflicts: [],
      narrowedTo: [],
      blockedFrom: [],
      varAssessment: null,
      decision: "NO_FOUL",
      severity: null,
      restart: "PLAY_CONTINUED",
      disciplinary: null,
      decisionMatch: "UNDETERMINED",
      confidence: "LOW",
      inconclusiveReason: null,
      factSignature: "0".repeat(64),
      factSignatureInput: "{}",
      citations: [citation],
    };
    expect(result.citations.length).toBeGreaterThanOrEqual(1);
  });

  it("오류 결과는 판정 결과와 다른 형태다", () => {
    const outcome: VarOutcome = {
      ok: false,
      error: "UNKNOWN_COMPETITION_OPTION",
      message: "corner_kick_review 채택 여부가 주어지지 않음",
    };
    expect(isEvaluationFailure(outcome)).toBe(true);
  });
});
```

- [x] **Step 2: 실패를 확인한다**

Run: `npx vitest run packages/shared-types/test/types.test.ts`
Expected: FAIL — `Cannot find module '../src/index.js'`

- [x] **Step 3: `citation.ts`를 쓴다**

```ts
import type { Authority, FactBlocker, FactStatus, Relevance } from "./vocabulary.js";

/**
 * 인용 하나만 떼어내도 출처를 확인할 수 있어야 한다.
 * 상위 객체에 authority가 있어도 여기서 생략하지 않는 이유다 (README 11절).
 */
export type RuleCitation = {
  ruleId: string;
  ruleRevision: number;
  ruleContentSha256: string;
  authority: Authority;
  edition: string;
  law: string; // "12" | "VAR"
  section: string;
  relevance: Relevance;
  quoteSnapshot: string;
  sourcePage: string | null;
};

/** 이 사실값이 정해지지 않으면 조항 트리에서 더 내려갈 수 없다. */
export type FactRequirement = {
  fact: string;
  status: FactStatus;
  /** null은 "관측했으나 값을 특정하지 못함". 관측 자체가 막힌 경우와 구분한다. */
  blockedBy: FactBlocker | null;
  narrowsTo: RuleCitation | null;
};

export type AuthorityAccount = {
  authority: Authority;
  edition: string;
  citations: RuleCitation[];
  requires: FactRequirement[];
};

/** 계층이 서로 다르게 적고 있을 때 — 해소하지 않고 노출한다. */
export type LayerConflict = {
  topic: string;
  readings: { authority: string; text: string }[];
};
```

- [x] **Step 4: `facts.ts`를 쓴다**

```ts
import type {
  CameraSufficiency,
  DecisionNature,
  DisplacementLevel,
  ErrorMagnitude,
  ObservationSpeed,
  ObservedSeverity,
  SendOffCategory,
  ReviewScenario,
} from "./vocabulary.js";

/** 사실값은 값과 관측 조건을 함께 가진다 (README 2절). */
export type Observed<T> = {
  value: T;
  observedAtSpeed: ObservationSpeed;
  shotIds: string[];
};

export const observed = <T>(
  value: T,
  observedAtSpeed: ObservationSpeed,
  shotIds: string[] = [],
): Observed<T> => ({ value, observedAtSpeed, shotIds });

export type PushFacts = {
  contactDetected: Observed<boolean>;
  severity: Observed<ObservedSeverity>;
  opponentDisplacement: Observed<DisplacementLevel>;
  insidePenaltyArea: Observed<boolean>;
  cameraSufficiency: CameraSufficiency;
};

export type VarFacts = {
  /** 중계에서 관측한 판정 상황. VAR 범주가 아니다 — 매핑은 판본 데이터가 갖는다. */
  reviewScenario: ReviewScenario;
  /** 검토 창을 닫는 유일한 조건. 주심이 판정을 내렸다는 사실은 차단 조건이 아니다. */
  restartOccurred: boolean;
  /** 퇴장 사안이 아니면 `"NONE"`. 재개 후 예외 판정(VAR 1.10)의 입력이다. */
  sendOffCategory: SendOffCategory;
  /**
   * 주심이 **다른 선수**에게 카드를 줬는가 (IFAB VAR 1.1.d).
   *
   * `reviewScenario`의 값이 아니라 별도 입력인 이유: 선수 확인 오류는 사건의
   * 종류가 아니라 카드 수여자의 속성이고, 경고와 퇴장 양쪽에서 일어난다.
   * 이 값이 MISTAKEN_IDENTITY 범주와 재개 후 예외 양쪽의 입구다.
   */
  mistakenIdentity: boolean;
  decisionNature: DecisionNature;
  errorMagnitude: ErrorMagnitude;
  /**
   * 주심단이 사건 자체를 보지 못했는가 (IFAB VAR 프로토콜 원문의
   * "a serious incident is missed/not seen by the match officials").
   *
   * `errorMagnitude`와 별개 입력이다. 원문이 문턱을 두 갈래의 OR로 적었고,
   * 정도 축에 섞으면 "놓친 사건"이 "덜 명백한 오심"으로 읽힌다.
   */
  seriousMissedIncident: boolean;
};

/**
 * 대회가 무엇을 채택했는가. 판본과는 별개 축이다.
 * 키가 없는 것(undefined)과 false는 다르다 — 모르는 것을 미채택으로 접지 않는다.
 */
export type CompetitionOptions = Readonly<Record<string, boolean | undefined>>;
```

- [x] **Step 5: `evaluation.ts`를 쓴다**

```ts
import type { AuthorityAccount, FactRequirement, LayerConflict, RuleCitation } from "./citation.js";
import type {
  ConfidenceLevel,
  Decision,
  DecisionMatch,
  DisciplinaryAction,
  EvaluationErrorCode,
  InconclusiveReason,
  RestartType,
  Severity,
  VarCategory,
  VarIntervention,
  VarNoInterventionReason,
  VarNotReviewableReason,
  VarReviewProcedure,
  VarThresholdResult,
  VarWindowClosedReason,
  VarWindowException,
} from "./vocabulary.js";

/**
 * 네 게이트를 각각 별도 필드로 둔다.
 * 합치면 정답을 내면서 이유를 틀린다 (README 11절, var-19).
 */
export type VarAssessment = {
  reviewable: boolean; // 게이트 1 결과
  category: VarCategory; // 게이트 1
  withinTimeWindow: boolean; // 게이트 2
  thresholdMet: VarThresholdResult; // 게이트 3
  reviewProcedure: VarReviewProcedure; // 게이트 4 — 문턱과 무관
  intervention: VarIntervention;
  /** 어느 게이트가 막았는가. */
  noInterventionReason: VarNoInterventionReason | null;
  /** 범주 게이트가 왜 막았는가. */
  notReviewableReason: VarNotReviewableReason | null;
  /** 시한 게이트가 왜 막았는가. */
  windowClosedReason: VarWindowClosedReason | null;
  /** NONE이 아니면 재개 여부와 무관하게 withinTimeWindow는 참이다. */
  windowException: VarWindowException;
  explanation: string;
};

export type EvaluationResult = {
  // 1. 규정이 이 상황에 대해 말하는 것
  accounts: AuthorityAccount[];
  conflicts: LayerConflict[];

  // 2. 조항 트리에서 도달한 깊이
  narrowedTo: RuleCitation[];
  blockedFrom: FactRequirement[];

  // 3. VAR 네 게이트 — 밀기 경로에서는 계산하지 않으므로 null
  varAssessment: VarAssessment | null;

  // 4. 참고 — 규정을 적용하면 나오는 판정
  decision: Decision;
  severity: Severity | null;
  restart: RestartType | null;
  disciplinary: DisciplinaryAction | null;
  decisionMatch: DecisionMatch;

  // 5. 한계
  confidence: ConfidenceLevel;
  inconclusiveReason: InconclusiveReason | null;

  // 6. 재현
  factSignature: string;
  factSignatureInput: string;

  citations: RuleCitation[]; // 비어 있을 수 없음
};

/** 입력이 부족하다는 신호. 판정 결과와 형태가 다르다. */
export type EvaluationFailure = {
  ok: false;
  error: EvaluationErrorCode;
  message: string;
};

export type VarOutcome =
  | {
      ok: true;
      assessment: VarAssessment;
      citations: RuleCitation[];
      factSignature: string;
      factSignatureInput: string;
    }
  | EvaluationFailure;

export const isEvaluationFailure = (outcome: VarOutcome): outcome is EvaluationFailure =>
  outcome.ok === false;
```

- [x] **Step 6: `rule-set.ts`를 쓴다**

```ts
import type { LayerConflict, RuleCitation } from "./citation.js";
import type { SendOffCategory, VarCategory, ReviewScenario } from "./vocabulary.js";

/**
 * 엔진이 데이터에게 묻는 것의 전부.
 * 엔진은 이 인터페이스만 알고 어느 판본을 받았는지는 모른다.
 */
export const CONCEPT_KEYS = Object.freeze([
  "LAW_12_DIRECT_FREE_KICK",
  "LAW_12_DISCIPLINE",
  "VAR_REVIEWABLE_CATEGORIES",
  "VAR_TIME_WINDOW",
  "VAR_THRESHOLD",
  "VAR_REVIEW_PROCESS",
] as const);
export type ConceptKey = (typeof CONCEPT_KEYS)[number];

export type VarCategoryRule = {
  id: VarCategory;
  appliesTo: readonly ReviewScenario[];
  /** 대회 채택 옵션 키. null이면 판본만으로 활성화된다. */
  requiresCompetitionOption: string | null;
  /**
   * 선수 확인 오류일 때만 성립하는 범주인가 (IFAB VAR 1.1.d).
   *
   * `appliesTo`로 표현할 수 없는 이유: CARD_SHOWN은 RED_CARD와
   * MISTAKEN_IDENTITY 양쪽의 appliesTo에 들어 있고, 둘을 가르는 것은
   * 사건의 종류가 아니라 카드를 받은 선수가 맞는지다.
   */
  requiresMistakenIdentity: boolean;
};

export type TimeWindowExceptions = {
  mistakenIdentity: boolean;
  sendOffCategories: readonly SendOffCategory[];
};

export type RuleSet = {
  cite(conceptKey: ConceptKey): RuleCitation[];
  varCategories(): readonly VarCategoryRule[];
  timeWindowExceptions(): TimeWindowExceptions;
  layerConflicts(): readonly LayerConflict[];
};
```

- [x] **Step 7: `index.ts`를 쓴다**

```ts
export * from "./vocabulary.js";
export * from "./citation.js";
export * from "./facts.js";
export * from "./evaluation.js";
export * from "./rule-set.js";
```

- [x] **Step 8: 통과를 확인한다**

Run: `npx vitest run packages/shared-types && npm run typecheck`
Expected: PASS — 9 케이스, 타입 오류 없음

---

### Task 3: `rule-data` — 2025/26 판본 데이터와 `loadRuleSet`

**Files:**
- Create: `packages/rule-data/package.json`, `packages/rule-data/tsconfig.json`
- Create: `packages/rule-data/data/ifab/2025-26.json`
- Create: `packages/rule-data/src/schema.ts`
- Create: `packages/rule-data/src/load-rule-set.ts`
- Create: `packages/rule-data/src/index.ts`
- Test: `packages/rule-data/test/load-rule-set.test.ts`

**Interfaces:**
- Consumes: Task 2의 `RuleSet` `RuleCitation` `ConceptKey` `VarCategoryRule` `TimeWindowExceptions` `LayerConflict`.
- Produces:
  - `loadRuleSet(versionId: string): RuleSet | null` — 알 수 없는 판본이면 `null`. 던지지 않는다.
  - `KNOWN_RULE_VERSION_IDS: readonly string[]`
  - `RuleSetFile` 타입 (JSON 파일 형태)

**이 태스크의 설계 결정 세 가지**

1. **`ruleContentSha256`은 손으로 적지 않고 적재 시 `quoteSnapshot`에서 계산한다.** 손으로 적은 해시는 검증할 수 없고, 발췌가 바뀌어도 조용히 남는다. 계산하면 발췌가 바뀌는 순간 해시가 바뀌고 과거 `decision_results`가 다른 값을 가리키게 된다 — 그게 이 열의 목적이다.
2. **JSON에는 `authority`와 `edition`을 인용마다 반복해 넣지 않고 파일 머리에 한 번 쓴다.** `loadRuleSet`이 반환할 때 모든 인용에 채워 넣으므로, 밖으로 나가는 `RuleCitation`은 README 11절대로 자기 출처를 전부 갖는다.
3. **`resolveRuleVersion`(대회 → IFAB 판본 매핑)은 1단계에 넣지 않는다.** README 3절의 매핑이 아직 `INFERRED`이고 15절의 "대회요강 판본 지정 범위 재확인"이 열려 있다. 확인되지 않은 매핑을 코드로 굳히면 추정이 사실로 승격된다. 1단계에서는 호출자가 판본 id를 직접 준다.

- [x] **Step 1: 패키지 파일 두 개를 만든다**

`packages/rule-data/package.json`:

```json
{
  "name": "@replay/rule-data",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts"
}
```

`packages/rule-data/tsconfig.json`:

```json
{
  "extends": "../../tsconfig.base.json",
  "include": ["src/**/*.ts", "test/**/*.ts", "data/**/*.json"]
}
```

- [x] **Step 2: 실패하는 테스트를 쓴다**

`packages/rule-data/test/load-rule-set.test.ts`:

```ts
import { createHash } from "node:crypto";
import { describe, expect, it } from "vitest";
import { KNOWN_RULE_VERSION_IDS, loadRuleSet } from "../src/index.js";

const ruleSet2025 = loadRuleSet("ifab-2025-26");

describe("loadRuleSet", () => {
  it("알 수 없는 판본에는 null을 반환하고 던지지 않는다", () => {
    expect(loadRuleSet("ifab-1998-99")).toBeNull();
    expect(KNOWN_RULE_VERSION_IDS).toContain("ifab-2025-26");
  });

  it("모든 인용이 자기 출처를 전부 갖는다", () => {
    expect(ruleSet2025).not.toBeNull();
    const citations = [
      ...ruleSet2025!.cite("LAW_12_DIRECT_FREE_KICK"),
      ...ruleSet2025!.cite("LAW_12_DISCIPLINE"),
      ...ruleSet2025!.cite("VAR_REVIEWABLE_CATEGORIES"),
      ...ruleSet2025!.cite("VAR_TIME_WINDOW"),
      ...ruleSet2025!.cite("VAR_THRESHOLD"),
      ...ruleSet2025!.cite("VAR_REVIEW_PROCESS"),
    ];
    expect(citations.length).toBeGreaterThan(0);
    for (const citation of citations) {
      expect(citation.authority).toBe("IFAB");
      expect(citation.edition).toBe("2025-26");
      expect(citation.ruleId).toMatch(/^ifab-2025-26-/);
      expect(citation.quoteSnapshot.length).toBeGreaterThan(0);
      expect(citation.ruleContentSha256).toBe(
        createHash("sha256").update(citation.quoteSnapshot, "utf8").digest("hex"),
      );
    }
  });

  it("2025/26의 검토 가능 범주는 네 개이고 2차 경고는 빠져 있다", () => {
    const categories = ruleSet2025!.varCategories();
    expect(categories.map((category) => category.id)).toEqual([
      "GOAL_NO_GOAL",
      "PENALTY_NO_PENALTY",
      "RED_CARD",
      "MISTAKEN_IDENTITY",
    ]);
    const redCard = categories.find((category) => category.id === "RED_CARD");
    expect(redCard?.appliesTo).toEqual(["CARD_SHOWN", "SENDING_OFF_NOT_GIVEN"]);
    expect(redCard?.appliesTo).not.toContain("SECOND_CAUTION");
    expect(categories.every((category) => category.requiresCompetitionOption === null)).toBe(true);
  });

  it("재개 예외 목록은 데이터가 소유한다", () => {
    expect(ruleSet2025!.timeWindowExceptions()).toEqual({
      mistakenIdentity: true,
      sendOffCategories: ["VIOLENT_CONDUCT", "BITING_OR_SPITTING", "OFFENSIVE_LANGUAGE_OR_ACTION"],
    });
  });

  it("확인되지 않은 계층 충돌을 지어내지 않는다", () => {
    expect(ruleSet2025!.layerConflicts()).toEqual([]);
  });

  it("반환된 RuleSet은 동결되어 있고 호출마다 같은 값을 준다", () => {
    const again = loadRuleSet("ifab-2025-26");
    expect(again).toBe(ruleSet2025);
    expect(Object.isFrozen(ruleSet2025)).toBe(true);
    expect(Object.isFrozen(ruleSet2025!.varCategories())).toBe(true);
  });
});
```

- [x] **Step 3: 실패를 확인한다**

Run: `npx vitest run packages/rule-data`
Expected: FAIL — `Cannot find module '../src/index.js'`

- [x] **Step 4: 2025/26 데이터 파일을 쓴다**

`packages/rule-data/data/ifab/2025-26.json` — 아래 인용문은 `rules/ifab/2025-26/Laws of the Game 2025_26_single pages.pdf`에서 발췌한 것이고 쪽번호는 그 PDF의 인쇄 쪽번호다. **조항 단위 발췌이며 원문 전문이 아니다.**

```json
{
  "versionId": "ifab-2025-26",
  "authority": "IFAB",
  "edition": "2025-26",
  "concepts": {
    "LAW_12_DIRECT_FREE_KICK": [
      {
        "key": "law-12-1-direct-free-kick",
        "revision": 1,
        "law": "12",
        "section": "1",
        "relevance": "PRIMARY",
        "sourcePage": "109",
        "quoteSnapshot": "A direct free kick is awarded if a player commits any of the following offences against an opponent in a manner considered by the referee to be careless, reckless or using excessive force: charges; jumps at; kicks or attempts to kick; pushes; strikes or attempts to strike (including head-butt); tackles or challenges; trips or attempts to trip. If an offence involves contact, it is penalised by a direct free kick."
      }
    ],
    "LAW_12_DISCIPLINE": [
      {
        "key": "law-12-1-discipline",
        "revision": 1,
        "law": "12",
        "section": "1",
        "relevance": "PRIMARY",
        "sourcePage": "109",
        "quoteSnapshot": "Careless is when a player shows a lack of attention or consideration when making a challenge or acts without precaution. No disciplinary sanction is needed. Reckless is when a player acts with disregard to the danger to, or consequences for, an opponent and must be cautioned. Using excessive force is when a player exceeds the necessary use of force and/or endangers the safety of an opponent and must be sent off."
      }
    ],
    "VAR_REVIEWABLE_CATEGORIES": [
      {
        "key": "var-1-1-categories",
        "revision": 1,
        "law": "VAR",
        "section": "1.1",
        "relevance": "PRIMARY",
        "sourcePage": "147",
        "quoteSnapshot": "A video assistant referee (VAR) is a match official, with independent access to match footage, who may assist the referee only in the event of a 'clear and obvious error' or 'serious missed incident' in relation to: a. Goal/no goal b. Penalty/no penalty c. Direct red card (not second yellow card/caution) d. Mistaken identity (when the referee cautions or sends off the wrong player of the offending team)"
      },
      {
        "key": "var-2-reviewable-decisions",
        "revision": 1,
        "law": "VAR",
        "section": "2",
        "relevance": "PRIMARY",
        "sourcePage": "148",
        "quoteSnapshot": "The referee may receive assistance from the VAR only in relation to four categories of match-changing decisions/incidents. In all these situations, the VAR is only used after the referee has made a (first/original) decision (including allowing play to continue), or if a serious incident is missed/not seen by the match officials."
      }
    ],
    "VAR_TIME_WINDOW": [
      {
        "key": "var-1-10-restart-closes-window",
        "revision": 1,
        "law": "VAR",
        "section": "1.10",
        "relevance": "PRIMARY",
        "sourcePage": "148",
        "quoteSnapshot": "If play has stopped and been restarted, the referee may not undertake a 'review' except for a case of mistaken identity or for a potential sending-off offence relating to violent conduct, spitting, biting or extremely offensive, insulting and/or abusive action(s)."
      },
      {
        "key": "var-1-2-original-decision-required",
        "revision": 1,
        "law": "VAR",
        "section": "1.2",
        "relevance": "SUPPORTING",
        "sourcePage": "147",
        "quoteSnapshot": "The referee must always make a decision, i.e. the referee is not permitted to give 'no decision' and then use the VAR to make the decision; a decision to allow play to continue after an alleged offence can be reviewed."
      }
    ],
    "VAR_THRESHOLD": [
      {
        "key": "var-1-3-clear-and-obvious",
        "revision": 1,
        "law": "VAR",
        "section": "1.3",
        "relevance": "PRIMARY",
        "sourcePage": "147",
        "quoteSnapshot": "The original decision given by the referee will not be changed unless the video review clearly shows that the decision was a 'clear and obvious error'."
      }
    ],
    "VAR_REVIEW_PROCESS": [
      {
        "key": "var-3-replay-speed",
        "revision": 1,
        "law": "VAR",
        "section": "3",
        "relevance": "PRIMARY",
        "sourcePage": "151",
        "quoteSnapshot": "The VAR can 'check' the footage in normal speed and/or in slow motion but, in general, slow-motion replays should only be used for facts, e.g. position of offence/player, point of contact for physical offences and handball, ball out of play (including goal/no goal); normal speed should be used for the 'intensity' of an offence or to decide if it was a handball offence"
      },
      {
        "key": "var-1-5-final-decision-ofr",
        "revision": 1,
        "law": "VAR",
        "section": "1.5",
        "relevance": "SUPPORTING",
        "sourcePage": "148",
        "quoteSnapshot": "The final decision is always taken by the referee, either based on information from the VAR or after the referee has undertaken an 'on-field review' (OFR)."
      }
    ]
  },
  "varCategories": [
    {
      "id": "GOAL_NO_GOAL",
      "appliesTo": ["GOAL_AWARDED", "GOAL_DISALLOWED"],
      "requiresCompetitionOption": null,
      "requiresMistakenIdentity": false
    },
    {
      "id": "PENALTY_NO_PENALTY",
      "appliesTo": ["PENALTY_GIVEN", "PENALTY_NOT_GIVEN"],
      "requiresCompetitionOption": null,
      "requiresMistakenIdentity": false
    },
    {
      "id": "RED_CARD",
      "appliesTo": ["CARD_SHOWN", "SENDING_OFF_NOT_GIVEN"],
      "requiresCompetitionOption": null,
      "requiresMistakenIdentity": false
    },
    {
      "id": "MISTAKEN_IDENTITY",
      "appliesTo": ["CARD_SHOWN", "SECOND_CAUTION", "SENDING_OFF_NOT_GIVEN"],
      "requiresCompetitionOption": null,
      "requiresMistakenIdentity": true
    }
  ],
  "timeWindowExceptions": {
    "mistakenIdentity": true,
    "sendOffCategories": ["VIOLENT_CONDUCT", "BITING_OR_SPITTING", "OFFENSIVE_LANGUAGE_OR_ACTION"]
  },
  "layerConflicts": []
}
```

- [x] **Step 5: `schema.ts`를 쓴다**

`packages/rule-data/src/schema.ts`:

```ts
import type {
  Authority,
  ConceptKey,
  LayerConflict,
  Relevance,
  SendOffCategory,
  VarCategory,
  ReviewScenario,
} from "@replay/shared-types";

/** JSON 파일에 저장되는 인용. authority와 edition은 파일 머리에서 온다. */
export type StoredCitation = {
  key: string;
  revision: number;
  law: string;
  section: string;
  relevance: Relevance;
  sourcePage: string | null;
  quoteSnapshot: string;
};

export type RuleSetFile = {
  versionId: string;
  authority: Authority;
  edition: string;
  concepts: Record<ConceptKey, StoredCitation[]>;
  varCategories: {
    id: VarCategory;
    appliesTo: ReviewScenario[];
    requiresCompetitionOption: string | null;
    requiresMistakenIdentity: boolean;
  }[];
  timeWindowExceptions: {
    mistakenIdentity: boolean;
    sendOffCategories: SendOffCategory[];
  };
  layerConflicts: LayerConflict[];
};
```

- [x] **Step 6: `load-rule-set.ts`를 쓴다**

`packages/rule-data/src/load-rule-set.ts`:

```ts
import { createHash } from "node:crypto";
import type { ConceptKey, RuleCitation, RuleSet } from "@replay/shared-types";
import ifab202526 from "../data/ifab/2025-26.json" with { type: "json" };
import type { RuleSetFile, StoredCitation } from "./schema.js";

const FILES: readonly RuleSetFile[] = [ifab202526 as RuleSetFile];

const materialize = (file: RuleSetFile, stored: StoredCitation): RuleCitation =>
  Object.freeze({
    ruleId: `${file.versionId}-${stored.key}`,
    ruleRevision: stored.revision,
    // 발췌가 바뀌면 해시가 바뀐다. 그래야 과거 판정이 어느 문구를 근거로 했는지 남는다.
    ruleContentSha256: createHash("sha256").update(stored.quoteSnapshot, "utf8").digest("hex"),
    authority: file.authority,
    edition: file.edition,
    law: stored.law,
    section: stored.section,
    relevance: stored.relevance,
    quoteSnapshot: stored.quoteSnapshot,
    sourcePage: stored.sourcePage,
  });

const build = (file: RuleSetFile): RuleSet => {
  const citations = new Map<ConceptKey, readonly RuleCitation[]>();
  for (const [conceptKey, stored] of Object.entries(file.concepts)) {
    citations.set(
      conceptKey as ConceptKey,
      Object.freeze(stored.map((entry) => materialize(file, entry))),
    );
  }

  const varCategories = Object.freeze(
    file.varCategories.map((category) =>
      Object.freeze({
        id: category.id,
        appliesTo: Object.freeze([...category.appliesTo]),
        requiresCompetitionOption: category.requiresCompetitionOption,
        requiresMistakenIdentity: category.requiresMistakenIdentity,
      }),
    ),
  );

  const timeWindowExceptions = Object.freeze({
    mistakenIdentity: file.timeWindowExceptions.mistakenIdentity,
    sendOffCategories: Object.freeze([...file.timeWindowExceptions.sendOffCategories]),
  });

  const layerConflicts = Object.freeze(file.layerConflicts.map((conflict) => Object.freeze(conflict)));

  return Object.freeze({
    cite: (conceptKey: ConceptKey) => [...(citations.get(conceptKey) ?? [])],
    varCategories: () => varCategories,
    timeWindowExceptions: () => timeWindowExceptions,
    layerConflicts: () => layerConflicts,
  });
};

const REGISTRY: ReadonlyMap<string, RuleSet> = new Map(
  FILES.map((file) => [file.versionId, build(file)]),
);

export const KNOWN_RULE_VERSION_IDS: readonly string[] = Object.freeze([...REGISTRY.keys()]);

/** 알 수 없는 판본이면 null. 던지지 않는 이유는 호출자가 UNKNOWN_RULE_VERSION으로 보고해야 하기 때문. */
export const loadRuleSet = (versionId: string): RuleSet | null => REGISTRY.get(versionId) ?? null;
```

`cite`가 배열 사본을 반환하는 이유: 호출자가 결과 배열을 `push`로 조립하는 경우가 있어 원본 동결 배열을 그대로 넘기면 런타임 오류가 난다.

- [x] **Step 7: `index.ts`를 쓴다**

```ts
export * from "./schema.js";
export * from "./load-rule-set.js";
```

- [x] **Step 8: 통과를 확인한다**

Run: `npx vitest run packages/rule-data && npm run typecheck`
Expected: PASS — 6 케이스, 타입 오류 없음

---

### Task 4: `build-accounts` — 게이트보다 먼저 채워지는 규정 설명

**Files:**
- Create: `packages/rule-engine/package.json`, `packages/rule-engine/tsconfig.json`
- Create: `packages/rule-engine/src/interpreter/build-accounts.ts`
- Test: `packages/rule-engine/test/build-accounts.test.ts`

**Interfaces:**
- Consumes: Task 2의 `PushFacts` `RuleSet` `AuthorityAccount` `FactRequirement` `LayerConflict` `RuleCitation`; Task 3의 `loadRuleSet`(테스트에서만).
- Produces:
  ```ts
  type PushAccountView = {
    accounts: AuthorityAccount[];
    conflicts: LayerConflict[];
    narrowedTo: RuleCitation[];
    blockedFrom: FactRequirement[];
  };
  buildPushAccounts(facts: PushFacts, rules: RuleSet): PushAccountView
  ```

**왜 이 함수가 게이트보다 먼저 도는가**

규정이 무엇을 요구하는지는 영상과 무관하게 말할 수 있다 (README 11절). 각도가 나빠서 아무 사실값도 못 세웠어도 "Law 12는 접촉·강도·위치를 요구한다"는 문장은 그대로 성립한다. 게이트에서 바로 반환해 버리면 이 문장이 화면에서 사라지고 사용자는 "판정 불가"만 본다. push-10이 이걸 검사한다.

- [x] **Step 1: 패키지 파일 두 개를 만든다**

`packages/rule-engine/package.json`:

```json
{
  "name": "@replay/rule-engine",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts"
}
```

`packages/rule-engine/tsconfig.json`:

```json
{
  "extends": "../../tsconfig.base.json",
  "include": ["src/**/*.ts", "test/**/*.ts", "fixtures/**/*.json"]
}
```

- [x] **Step 2: 실패하는 테스트를 쓴다**

`packages/rule-engine/test/build-accounts.test.ts`:

```ts
import type { PushFacts } from "@replay/shared-types";
import { observed } from "@replay/shared-types";
import { loadRuleSet } from "@replay/rule-data";
import { describe, expect, it } from "vitest";
import { buildPushAccounts } from "../src/interpreter/build-accounts.js";

const rules = loadRuleSet("ifab-2025-26")!;

const establishedFacts: PushFacts = {
  contactDetected: observed(true, "NORMAL", ["shot-1"]),
  severity: observed("RECKLESS", "NORMAL", ["shot-1"]),
  opponentDisplacement: observed("clear", "NORMAL", ["shot-1"]),
  insidePenaltyArea: observed(false, "NORMAL", ["shot-1"]),
  cameraSufficiency: "HIGH",
};

const requirementFor = (facts: PushFacts, name: string) =>
  buildPushAccounts(facts, rules).accounts[0]!.requires.find((entry) => entry.fact === name)!;

describe("buildPushAccounts", () => {
  it("사실값이 전부 서면 blockedFrom이 비고 징계 조항까지 내려간다", () => {
    const view = buildPushAccounts(establishedFacts, rules);
    expect(view.accounts).toHaveLength(1);
    expect(view.accounts[0]!.authority).toBe("IFAB");
    expect(view.blockedFrom).toEqual([]);
    expect(view.narrowedTo.map((citation) => citation.section)).toContain("1");
    expect(view.narrowedTo.length).toBeGreaterThanOrEqual(2);
  });

  it("각도가 부족해도 accounts는 비지 않고 요구 사실이 그대로 나열된다", () => {
    const view = buildPushAccounts({ ...establishedFacts, cameraSufficiency: "LOW" }, rules);
    expect(view.accounts).toHaveLength(1);
    expect(view.accounts[0]!.citations.length).toBeGreaterThan(0);
    expect(view.accounts[0]!.requires.map((entry) => entry.fact)).toEqual([
      "contactDetected",
      "severity",
      "opponentDisplacement",
      "insidePenaltyArea",
    ]);
    expect(view.blockedFrom).toHaveLength(4);
    expect(view.blockedFrom.every((entry) => entry.blockedBy === "CAMERA")).toBe(true);
  });

  it("슬로우모션에서만 본 강도는 SPEED로 막힌다", () => {
    const facts: PushFacts = {
      ...establishedFacts,
      severity: observed("EXCESSIVE_FORCE", "SLOW", ["shot-2"]),
    };
    const severity = requirementFor(facts, "severity");
    expect(severity.status).toBe("UNMET");
    expect(severity.blockedBy).toBe("SPEED");
    expect(requirementFor(facts, "contactDetected").status).toBe("ESTABLISHED");
  });

  it("정상 속도로 봤지만 값을 특정 못한 경우는 blockedBy가 null이다", () => {
    const facts: PushFacts = {
      ...establishedFacts,
      severity: observed("uncertain", "NORMAL", ["shot-1"]),
    };
    const severity = requirementFor(facts, "severity");
    expect(severity.status).toBe("UNMET");
    expect(severity.blockedBy).toBeNull();
  });

  it("밀림이 possible이면 관측은 됐어도 요구 사실은 서지 않는다", () => {
    const facts: PushFacts = {
      ...establishedFacts,
      opponentDisplacement: observed("possible", "NORMAL", ["shot-1"]),
    };
    const displacement = requirementFor(facts, "opponentDisplacement");
    // 게이트가 possible에서 보류하는데 요구 사실만 서면 blockedFrom이 빈 채로
    // INCONCLUSIVE가 나간다. 게이트와 요구사항이 같은 값을 같게 봐야 한다.
    expect(displacement.status).toBe("UNMET");
    expect(displacement.blockedBy).toBeNull();
    expect(buildPushAccounts(facts, rules).blockedFrom).toHaveLength(1);
  });

  it("강도가 막히면 징계 조항으로 내려가지 못하고 narrowsTo가 그 조항을 가리킨다", () => {
    const facts: PushFacts = {
      ...establishedFacts,
      severity: observed("RECKLESS", "SLOW", ["shot-2"]),
    };
    const view = buildPushAccounts(facts, rules);
    const severity = view.accounts[0]!.requires.find((entry) => entry.fact === "severity")!;
    expect(severity.narrowsTo?.ruleId).toBe("ifab-2025-26-law-12-1-discipline");
    expect(view.narrowedTo.map((citation) => citation.ruleId)).not.toContain(
      "ifab-2025-26-law-12-1-discipline",
    );
  });

  it("확인되지 않은 계층 충돌을 만들어내지 않는다", () => {
    expect(buildPushAccounts(establishedFacts, rules).conflicts).toEqual([]);
  });
});
```

- [x] **Step 3: 실패를 확인한다**

Run: `npx vitest run packages/rule-engine/test/build-accounts.test.ts`
Expected: FAIL — `Cannot find module '../src/interpreter/build-accounts.js'`

- [x] **Step 4: `build-accounts.ts`를 쓴다**

```ts
import type {
  AuthorityAccount,
  FactBlocker,
  FactRequirement,
  LayerConflict,
  Observed,
  PushFacts,
  RuleCitation,
  RuleSet,
} from "@replay/shared-types";

export type PushAccountView = {
  accounts: AuthorityAccount[];
  conflicts: LayerConflict[];
  narrowedTo: RuleCitation[];
  blockedFrom: FactRequirement[];
};

/**
 * 규정 질문에 답하지 못하는 표시값.
 *   uncertain — 관측했으나 값을 특정하지 못함
 *   possible  — 값은 정해졌으나 "밀렸는가"라는 규정 질문에는 답하지 않음
 *
 * 게이트 4가 둘을 같이 보류로 다룬다. 요구사항 판정이 여기서 갈라지면
 * decision은 INCONCLUSIVE인데 blockedFrom은 빈 상태가 만들어지고,
 * 사용자는 이유가 붙지 않은 '판정 불가'만 보게 된다 (push-10이 검사한다).
 */
const UNRESOLVED_VALUES: ReadonlySet<unknown> = new Set(["uncertain", "possible"]);

/**
 * 사실값 하나가 선 상태인지 판정한다.
 *
 * 세 가지를 구분한다.
 *   CAMERA — 각도가 부족해 관측 자체를 신뢰할 수 없음
 *   SPEED  — 정상 속도 관측이 없어 강도값을 입력으로 승인할 수 없음
 *   null   — 관측은 됐으나 그 값이 규정 질문에 답하지 못함
 * 셋을 합치면 사용자가 다음에 무엇을 해야 하는지가 사라진다.
 */
const requirement = (
  fact: string,
  observation: Observed<unknown>,
  options: { cameraSufficiency: PushFacts["cameraSufficiency"]; needsNormalSpeed: boolean },
  narrowsTo: RuleCitation | null,
): FactRequirement => {
  let blockedBy: FactBlocker | null = null;
  let established = true;

  if (options.cameraSufficiency === "LOW") {
    blockedBy = "CAMERA";
    established = false;
  } else if (options.needsNormalSpeed && observation.observedAtSpeed !== "NORMAL") {
    blockedBy = "SPEED";
    established = false;
  } else if (UNRESOLVED_VALUES.has(observation.value)) {
    established = false;
  }

  return {
    fact,
    status: established ? "ESTABLISHED" : "UNMET",
    blockedBy,
    narrowsTo,
  };
};

/**
 * 규정이 이 상황에 대해 말하는 것을 먼저 만든다.
 * 게이트는 이 결과를 지우지 않고 decision 계열 필드만 정한다.
 */
export const buildPushAccounts = (facts: PushFacts, rules: RuleSet): PushAccountView => {
  const offenceCitations = rules.cite("LAW_12_DIRECT_FREE_KICK");
  const disciplineCitations = rules.cite("LAW_12_DISCIPLINE");
  const speedCitations = rules.cite("VAR_REVIEW_PROCESS");
  const disciplineEntry = disciplineCitations[0] ?? null;

  const camera = { cameraSufficiency: facts.cameraSufficiency, needsNormalSpeed: false };
  const cameraAndSpeed = { cameraSufficiency: facts.cameraSufficiency, needsNormalSpeed: true };

  const requires: FactRequirement[] = [
    requirement("contactDetected", facts.contactDetected, camera, offenceCitations[0] ?? null),
    // 강도만 정상 속도를 요구한다 — VAR 프로토콜이 'intensity'에 normal speed를 요구하기 때문
    requirement("severity", facts.severity, cameraAndSpeed, disciplineEntry),
    requirement("opponentDisplacement", facts.opponentDisplacement, camera, disciplineEntry),
    requirement("insidePenaltyArea", facts.insidePenaltyArea, camera, offenceCitations[0] ?? null),
  ];

  const blockedFrom = requires.filter((entry) => entry.status === "UNMET");
  const severityEstablished =
    requires.find((entry) => entry.fact === "severity")?.status === "ESTABLISHED";

  const narrowedTo: RuleCitation[] = [...offenceCitations];
  if (severityEstablished) {
    narrowedTo.push(...disciplineCitations);
  } else if (facts.severity.observedAtSpeed !== "NORMAL" && facts.cameraSufficiency !== "LOW") {
    // 속도 게이트에 걸렸다는 사실 자체의 근거를 남긴다
    narrowedTo.push(...speedCitations.filter((citation) => citation.relevance === "PRIMARY"));
  }

  const accounts: AuthorityAccount[] = offenceCitations[0]
    ? [
        {
          authority: offenceCitations[0].authority,
          edition: offenceCitations[0].edition,
          citations: [...offenceCitations, ...disciplineCitations],
          requires,
        },
      ]
    : [];

  return { accounts, conflicts: [...rules.layerConflicts()], narrowedTo, blockedFrom };
};
```

- [x] **Step 5: 통과를 확인한다**

Run: `npx vitest run packages/rule-engine/test/build-accounts.test.ts`
Expected: PASS — 6 케이스

---

### Task 5: 각도 게이트·속도 게이트와 밀기 적용표

**Files:**
- Create: `packages/rule-engine/src/gates/push-gates.ts`
- Test: `packages/rule-engine/test/gates.test.ts`

**Interfaces:**
- Consumes: Task 2의 `PushFacts` `RuleSet` `RuleCitation` 및 어휘 타입.
- Produces:
  ```ts
  type PushVerdict = {
    decision: Decision;
    severity: Severity | null;
    restart: RestartType | null;
    disciplinary: DisciplinaryAction | null;
    confidence: ConfidenceLevel;
    inconclusiveReason: InconclusiveReason | null;
    citations: RuleCitation[];   // 최소 1개
  };
  applyPushGates(facts: PushFacts, rules: RuleSet): PushVerdict
  disciplinaryFor(severity: Severity): DisciplinaryAction
  ```

**게이트 순서 (README 11절 그대로)**

```text
1 각도 게이트   cameraSufficiency === "LOW"            → INCONCLUSIVE / CAMERA_INSUFFICIENT
2 접촉 없음     !contactDetected.value                  → NO_FOUL / restart PLAY_CONTINUED
3 속도 게이트   severity.observedAtSpeed !== "NORMAL"   → INCONCLUSIVE / SLOW_MOTION_ONLY
4 강도 미특정   severity uncertain 또는
               displacement uncertain·possible          → INCONCLUSIVE / SEVERITY_UNDETERMINED
5 접촉은 있으나 밀림 없음 + CARELESS                     → NORMAL_CONTACT / restart PLAY_CONTINUED
6 나머지                                                 → FOUL / 지역별 재개 + 강도별 징계
```

접촉 확인은 속도 게이트를 받지 않는다. VAR 프로토콜 원문이 슬로우모션을 `point of contact for physical offences`에 쓰라고 적고 있고, 정상 속도를 요구하는 것은 `intensity`다. 접촉 유무에까지 속도 게이트를 걸면 규정에 없는 제약을 만드는 것이 된다.

`possible`이 파울이 아니라 보류인 이유: 밀림이 "있었을 수도 있다"는 관측은 파울 성립 근거가 아니다. 여기서 파울로 넘기면 관측하지 않은 것을 관측한 것처럼 답하게 된다.

- [x] **Step 1: 실패하는 테스트를 쓴다**

`packages/rule-engine/test/gates.test.ts`:

```ts
import type { PushFacts } from "@replay/shared-types";
import { observed } from "@replay/shared-types";
import { loadRuleSet } from "@replay/rule-data";
import { describe, expect, it } from "vitest";
import { applyPushGates, disciplinaryFor } from "../src/gates/push-gates.js";

const rules = loadRuleSet("ifab-2025-26")!;

const base: PushFacts = {
  contactDetected: observed(true, "NORMAL", ["shot-1"]),
  severity: observed("RECKLESS", "NORMAL", ["shot-1"]),
  opponentDisplacement: observed("clear", "NORMAL", ["shot-1"]),
  insidePenaltyArea: observed(false, "NORMAL", ["shot-1"]),
  cameraSufficiency: "HIGH",
};

describe("applyPushGates", () => {
  it("각도가 부족하면 어떤 사실값이 들어와도 INCONCLUSIVE로 고정된다", () => {
    const verdict = applyPushGates({ ...base, cameraSufficiency: "LOW" }, rules);
    expect(verdict.decision).toBe("INCONCLUSIVE");
    expect(verdict.inconclusiveReason).toBe("CAMERA_INSUFFICIENT");
    expect(verdict.severity).toBeNull();
    expect(verdict.confidence).toBe("LOW");
  });

  it("접촉이 없으면 파울이 아니며 속도 게이트를 거치지 않는다", () => {
    const verdict = applyPushGates(
      {
        ...base,
        contactDetected: observed(false, "SLOW", ["shot-2"]),
        severity: observed("uncertain", "SLOW", ["shot-2"]),
      },
      rules,
    );
    expect(verdict.decision).toBe("NO_FOUL");
    expect(verdict.restart).toBe("PLAY_CONTINUED");
  });

  it("강도를 슬로우모션에서만 봤으면 판정하지 않고 근거로 VAR 프로토콜을 든다", () => {
    const verdict = applyPushGates(
      { ...base, severity: observed("EXCESSIVE_FORCE", "SLOW", ["shot-2"]) },
      rules,
    );
    expect(verdict.decision).toBe("INCONCLUSIVE");
    expect(verdict.inconclusiveReason).toBe("SLOW_MOTION_ONLY");
    expect(verdict.citations.some((citation) => citation.law === "VAR")).toBe(true);
  });

  it("관측 속도가 UNKNOWN인 강도도 승인하지 않는다", () => {
    const verdict = applyPushGates(
      { ...base, severity: observed("RECKLESS", "UNKNOWN", []) },
      rules,
    );
    expect(verdict.inconclusiveReason).toBe("SLOW_MOTION_ONLY");
  });

  it("밀림이 possible이면 파울로 단정하지 않는다", () => {
    const verdict = applyPushGates(
      { ...base, opponentDisplacement: observed("possible", "NORMAL", ["shot-1"]) },
      rules,
    );
    expect(verdict.decision).toBe("INCONCLUSIVE");
    expect(verdict.inconclusiveReason).toBe("SEVERITY_UNDETERMINED");
  });

  it("밀림 없는 부주의 접촉은 정상적인 몸싸움이다", () => {
    const verdict = applyPushGates(
      {
        ...base,
        severity: observed("CARELESS", "NORMAL", ["shot-1"]),
        opponentDisplacement: observed("none", "NORMAL", ["shot-1"]),
      },
      rules,
    );
    expect(verdict.decision).toBe("NORMAL_CONTACT");
    expect(verdict.restart).toBe("PLAY_CONTINUED");
    expect(verdict.disciplinary).toBe("NONE");
  });

  it("페널티지역 안팎이 재개 방식을 가른다", () => {
    expect(applyPushGates(base, rules).restart).toBe("DIRECT_FREE_KICK");
    expect(
      applyPushGates({ ...base, insidePenaltyArea: observed(true, "NORMAL", ["shot-1"]) }, rules)
        .restart,
    ).toBe("PENALTY_KICK");
  });

  it("강도가 징계 등급을 정한다", () => {
    expect(disciplinaryFor("CARELESS")).toBe("NONE");
    expect(disciplinaryFor("RECKLESS")).toBe("CAUTION");
    expect(disciplinaryFor("EXCESSIVE_FORCE")).toBe("SEND_OFF");
    const sendOff = applyPushGates(
      { ...base, severity: observed("EXCESSIVE_FORCE", "NORMAL", ["shot-1"]) },
      rules,
    );
    expect(sendOff.decision).toBe("FOUL");
    expect(sendOff.disciplinary).toBe("SEND_OFF");
    expect(sendOff.severity).toBe("EXCESSIVE_FORCE");
  });

  it("어떤 경로로 나가도 인용이 최소 하나 붙는다", () => {
    const inputs: PushFacts[] = [
      { ...base, cameraSufficiency: "LOW" },
      { ...base, contactDetected: observed(false, "NORMAL", ["shot-1"]) },
      { ...base, severity: observed("RECKLESS", "SLOW", ["shot-2"]) },
      { ...base, severity: observed("uncertain", "NORMAL", ["shot-1"]) },
      { ...base, severity: observed("CARELESS", "NORMAL", ["shot-1"]),
        opponentDisplacement: observed("none", "NORMAL", ["shot-1"]) },
      base,
    ];
    for (const facts of inputs) {
      expect(applyPushGates(facts, rules).citations.length).toBeGreaterThanOrEqual(1);
    }
  });
});
```

- [x] **Step 2: 실패를 확인한다**

Run: `npx vitest run packages/rule-engine/test/gates.test.ts`
Expected: FAIL — `Cannot find module '../src/gates/push-gates.js'`

- [x] **Step 3: `push-gates.ts`를 쓴다**

```ts
import type {
  ConfidenceLevel,
  Decision,
  DisciplinaryAction,
  InconclusiveReason,
  PushFacts,
  RestartType,
  RuleCitation,
  RuleSet,
  Severity,
} from "@replay/shared-types";

export type PushVerdict = {
  decision: Decision;
  severity: Severity | null;
  restart: RestartType | null;
  disciplinary: DisciplinaryAction | null;
  confidence: ConfidenceLevel;
  inconclusiveReason: InconclusiveReason | null;
  citations: RuleCitation[];
};

export const disciplinaryFor = (severity: Severity): DisciplinaryAction => {
  switch (severity) {
    case "CARELESS":
      return "NONE";
    case "RECKLESS":
      return "CAUTION";
    case "EXCESSIVE_FORCE":
      return "SEND_OFF";
  }
};

const confidenceFor = (facts: PushFacts): ConfidenceLevel =>
  facts.cameraSufficiency === "HIGH" ? "HIGH" : "MEDIUM";

const inconclusive = (
  reason: InconclusiveReason,
  citations: RuleCitation[],
): PushVerdict => ({
  decision: "INCONCLUSIVE",
  severity: null,
  restart: null,
  disciplinary: null,
  confidence: "LOW",
  inconclusiveReason: reason,
  citations,
});

/**
 * 게이트는 decision 계열 필드만 정한다.
 * accounts와 narrowedTo는 buildPushAccounts가 이미 만들어 두었고 여기서 지우지 않는다.
 */
export const applyPushGates = (facts: PushFacts, rules: RuleSet): PushVerdict => {
  const offence = rules.cite("LAW_12_DIRECT_FREE_KICK");
  const discipline = rules.cite("LAW_12_DISCIPLINE");
  const reviewProcess = rules.cite("VAR_REVIEW_PROCESS");

  // 1 각도 게이트 — 근거가 부족하면 판정하지 않는다
  if (facts.cameraSufficiency === "LOW") {
    return inconclusive("CAMERA_INSUFFICIENT", offence);
  }

  // 2 접촉 없음 — 슬로우모션 관측으로 충분하다 (원문이 point of contact를 사실 범주로 둔다)
  if (!facts.contactDetected.value) {
    return {
      decision: "NO_FOUL",
      severity: null,
      restart: "PLAY_CONTINUED",
      disciplinary: null,
      confidence: confidenceFor(facts),
      inconclusiveReason: null,
      citations: offence,
    };
  }

  // 3 속도 게이트 — 강도는 정상 속도 관측 없이 판정 입력으로 승인하지 않는다
  if (facts.severity.observedAtSpeed !== "NORMAL") {
    return inconclusive("SLOW_MOTION_ONLY", [...offence, ...reviewProcess]);
  }

  // 4 강도나 밀림을 특정할 수 없으면 보류한다. possible은 파울이 아니라 보류다
  if (
    facts.severity.value === "uncertain" ||
    facts.opponentDisplacement.value === "uncertain" ||
    facts.opponentDisplacement.value === "possible"
  ) {
    return inconclusive("SEVERITY_UNDETERMINED", [...offence, ...discipline]);
  }

  const severity = facts.severity.value;

  // 5 접촉은 있으나 밀림이 없고 부주의 수준이면 반칙이 아니다
  if (facts.opponentDisplacement.value === "none" && severity === "CARELESS") {
    return {
      decision: "NORMAL_CONTACT",
      severity: null,
      restart: "PLAY_CONTINUED",
      disciplinary: "NONE",
      confidence: confidenceFor(facts),
      inconclusiveReason: null,
      citations: [...offence, ...discipline],
    };
  }

  // 6 파울 — 재개는 지역이, 징계는 강도가 정한다
  return {
    decision: "FOUL",
    severity,
    restart: facts.insidePenaltyArea.value ? "PENALTY_KICK" : "DIRECT_FREE_KICK",
    disciplinary: disciplinaryFor(severity),
    confidence: confidenceFor(facts),
    inconclusiveReason: null,
    citations: [...offence, ...discipline],
  };
};
```

- [x] **Step 4: 통과를 확인한다**

Run: `npx vitest run packages/rule-engine/test/gates.test.ts`
Expected: PASS — 9 케이스

---

### Task 6: `fact-signature` — 결정적이고 식별자가 없는 서명

**Files:**
- Create: `packages/rule-engine/src/signatures/fact-signature.ts`
- Test: `packages/rule-engine/test/fact-signature.test.ts`

**Interfaces:**
- Consumes: 없음 (`node:crypto`만).
- Produces:
  ```ts
  type FactSignature = { signature: string; input: string };
  buildFactSignature(facts: Record<string, unknown>): FactSignature
  FORBIDDEN_SIGNATURE_KEY_PATTERN: RegExp
  ```

**서명에 들어가는 것과 빠지는 것**

| | 서명에 포함 | 이유 |
|---|---|---|
| 사실값 (`value`) | 포함 | 판정에 실제로 쓰인 입력 |
| 관측 속도 (`observedAtSpeed`) | 포함 | 같은 값이라도 속도가 다르면 승인 여부가 달라진다 |
| `cameraSufficiency` | 포함 | 각도 게이트의 입력 |
| `shotIds` | **제외** | 같은 사실 조합이 다른 영상에서 나와도 같은 서명이어야 조회가 성립한다 |
| 경기·심판·팀·선수·날짜 | **제외** | 협상 대상이 아님 (README 2절) |

`input`(정규화 문자열)을 함께 반환하는 이유는 var-20이 서명의 원문에 식별자가 없음을 확인해야 하기 때문이다. 해시만 반환하면 그 검사를 할 수 없다.

- [x] **Step 1: 실패하는 테스트를 쓴다**

`packages/rule-engine/test/fact-signature.test.ts`:

```ts
import { describe, expect, it } from "vitest";
import { buildFactSignature, FORBIDDEN_SIGNATURE_KEY_PATTERN } from "../src/signatures/fact-signature.js";

describe("buildFactSignature", () => {
  it("같은 입력은 항상 같은 64자리 서명을 낸다", () => {
    const first = buildFactSignature({ severity: "RECKLESS", insidePenaltyArea: false });
    const second = buildFactSignature({ severity: "RECKLESS", insidePenaltyArea: false });
    expect(first.signature).toBe(second.signature);
    expect(first.signature).toMatch(/^[0-9a-f]{64}$/);
  });

  it("키 순서가 달라도 같은 서명을 낸다", () => {
    const a = buildFactSignature({ severity: "RECKLESS", insidePenaltyArea: false });
    const b = buildFactSignature({ insidePenaltyArea: false, severity: "RECKLESS" });
    expect(a.signature).toBe(b.signature);
  });

  it("값이 하나만 달라도 서명이 달라진다", () => {
    const a = buildFactSignature({ severity: "RECKLESS" });
    const b = buildFactSignature({ severity: "CARELESS" });
    expect(a.signature).not.toBe(b.signature);
  });

  it("shotIds는 서명에서 빠진다", () => {
    const a = buildFactSignature({ severity: "RECKLESS", shotIds: ["shot-1"] });
    const b = buildFactSignature({ severity: "RECKLESS", shotIds: ["shot-9", "shot-12"] });
    expect(a.signature).toBe(b.signature);
    expect(a.input).not.toContain("shot-1");
  });

  it("중첩된 관측 객체도 정규화한다", () => {
    const signature = buildFactSignature({
      severity: { value: "RECKLESS", observedAtSpeed: "NORMAL", shotIds: ["shot-3"] },
    });
    expect(signature.input).toContain("observedAtSpeed");
    expect(signature.input).not.toContain("shot-3");
  });

  it("경기·심판 식별자를 담은 키는 거부한다", () => {
    expect(() => buildFactSignature({ matchId: "x", severity: "RECKLESS" })).toThrow(
      /matchId/,
    );
    expect(() => buildFactSignature({ refereeName: "x" })).toThrow(/refereeName/);
    for (const key of ["matchId", "referee", "officialId", "playerName", "teamId", "kickoffDate"]) {
      expect(FORBIDDEN_SIGNATURE_KEY_PATTERN.test(key)).toBe(true);
    }
    expect(FORBIDDEN_SIGNATURE_KEY_PATTERN.test("severity")).toBe(false);
  });
});
```

- [x] **Step 2: 실패를 확인한다**

Run: `npx vitest run packages/rule-engine/test/fact-signature.test.ts`
Expected: FAIL — `Cannot find module '../src/signatures/fact-signature.js'`

- [x] **Step 3: `fact-signature.ts`를 쓴다**

```ts
import { createHash } from "node:crypto";

export type FactSignature = {
  signature: string;
  /** 해시의 원문. 서명에 무엇이 들어갔는지 검사할 수 있게 함께 반환한다. */
  input: string;
};

/**
 * 서명에 들어가서는 안 되는 키.
 * 이 목록은 축소하지 않는다 — 서명은 사건이 아니라 사실 조합을 가리켜야 한다.
 */
export const FORBIDDEN_SIGNATURE_KEY_PATTERN =
  /(match|referee|official|player|team|club|venue|stadium|kickoff|date|season|fixture)/i;

/** 증거 포인터일 뿐 사실값이 아니므로 서명에서 제외한다. */
const EXCLUDED_KEYS = new Set(["shotIds"]);

const normalize = (value: unknown, path: string): unknown => {
  if (Array.isArray(value)) {
    return value.map((entry, index) => normalize(entry, `${path}[${index}]`));
  }
  if (value !== null && typeof value === "object") {
    const entries = Object.entries(value as Record<string, unknown>)
      .filter(([key]) => !EXCLUDED_KEYS.has(key))
      .sort(([left], [right]) => (left < right ? -1 : left > right ? 1 : 0));

    const normalized: Record<string, unknown> = {};
    for (const [key, entryValue] of entries) {
      if (FORBIDDEN_SIGNATURE_KEY_PATTERN.test(key)) {
        throw new Error(
          `fact_signature에 식별자로 보이는 키를 넣을 수 없음: ${path ? `${path}.` : ""}${key}`,
        );
      }
      normalized[key] = normalize(entryValue, path ? `${path}.${key}` : key);
    }
    return normalized;
  }
  return value;
};

export const buildFactSignature = (facts: Record<string, unknown>): FactSignature => {
  const input = JSON.stringify(normalize(facts, ""));
  return {
    signature: createHash("sha256").update(input, "utf8").digest("hex"),
    input,
  };
};
```

`JSON.stringify`가 키 순서를 보존하므로 `normalize`가 정렬해 두면 그대로 정규 형태가 된다.

- [x] **Step 4: 통과를 확인한다**

Run: `npx vitest run packages/rule-engine/test/fact-signature.test.ts`
Expected: PASS — 6 케이스

---

### Task 7: `evaluatePush` 조립

**Files:**
- Create: `packages/rule-engine/src/evaluators/pushing/evaluate-push.ts`
- Create: `packages/rule-engine/src/index.ts`
- Test: `packages/rule-engine/test/evaluate-push.test.ts`

**Interfaces:**
- Consumes: Task 4의 `buildPushAccounts`, Task 5의 `applyPushGates`, Task 6의 `buildFactSignature`.
- Produces: `evaluatePush(facts: PushFacts, rules: RuleSet): EvaluationResult`. `packages/rule-engine/src/index.ts`가 `evaluatePush` `buildPushAccounts` `applyPushGates` `disciplinaryFor` `buildFactSignature` `FORBIDDEN_SIGNATURE_KEY_PATTERN`과 각 타입을 재수출한다.

`decisionMatch`는 1단계에서 항상 `"UNDETERMINED"`다. 비교 대상인 관측 판정(`observed_*`)이 아직 입력에 없다. 없는 비교를 `MATCH`로 채우면 화면이 "규정과 일치함"이라고 말하게 되는데 아무것도 비교하지 않았다.

- [x] **Step 1: 실패하는 테스트를 쓴다**

`packages/rule-engine/test/evaluate-push.test.ts`:

```ts
import type { PushFacts } from "@replay/shared-types";
import { observed } from "@replay/shared-types";
import { loadRuleSet } from "@replay/rule-data";
import { describe, expect, it } from "vitest";
import { evaluatePush } from "../src/index.js";

const rules = loadRuleSet("ifab-2025-26")!;

const base: PushFacts = {
  contactDetected: observed(true, "NORMAL", ["shot-1"]),
  severity: observed("RECKLESS", "NORMAL", ["shot-1"]),
  opponentDisplacement: observed("clear", "NORMAL", ["shot-1"]),
  insidePenaltyArea: observed(false, "NORMAL", ["shot-1"]),
  cameraSufficiency: "HIGH",
};

describe("evaluatePush", () => {
  it("사실값이 막혀도 규정 설명은 그대로 나온다", () => {
    const result = evaluatePush({ ...base, cameraSufficiency: "LOW" }, rules);
    expect(result.decision).toBe("INCONCLUSIVE");
    expect(result.accounts).not.toHaveLength(0);
    expect(result.accounts[0]!.requires).not.toHaveLength(0);
    expect(result.blockedFrom).not.toHaveLength(0);
    expect(result.citations.length).toBeGreaterThanOrEqual(1);
  });

  it("밀기 경로에서는 VAR 판단을 계산하지 않는다", () => {
    expect(evaluatePush(base, rules).varAssessment).toBeNull();
  });

  it("인용은 ruleId 기준으로 중복 없이 모인다", () => {
    const ids = evaluatePush(base, rules).citations.map((citation) => citation.ruleId);
    expect(new Set(ids).size).toBe(ids.length);
  });

  it("서명은 결정적이고 shotIds에 흔들리지 않는다", () => {
    const first = evaluatePush(base, rules);
    const second = evaluatePush(
      { ...base, contactDetected: observed(true, "NORMAL", ["shot-77"]) },
      rules,
    );
    expect(first.factSignature).toBe(second.factSignature);
    expect(first.factSignature).toMatch(/^[0-9a-f]{64}$/);
  });

  it("관측 판정이 없으므로 비교 결과는 UNDETERMINED다", () => {
    expect(evaluatePush(base, rules).decisionMatch).toBe("UNDETERMINED");
  });

  it("파울 경로는 강도·재개·징계를 함께 채운다", () => {
    const result = evaluatePush(
      { ...base, insidePenaltyArea: observed(true, "NORMAL", ["shot-1"]) },
      rules,
    );
    expect(result.decision).toBe("FOUL");
    expect(result.severity).toBe("RECKLESS");
    expect(result.restart).toBe("PENALTY_KICK");
    expect(result.disciplinary).toBe("CAUTION");
    expect(result.inconclusiveReason).toBeNull();
  });
});
```

- [x] **Step 2: 실패를 확인한다**

Run: `npx vitest run packages/rule-engine/test/evaluate-push.test.ts`
Expected: FAIL — `Cannot find module '../src/index.js'`

- [x] **Step 3: `evaluate-push.ts`를 쓴다**

```ts
import type { EvaluationResult, PushFacts, RuleCitation, RuleSet } from "@replay/shared-types";
import { buildPushAccounts } from "../../interpreter/build-accounts.js";
import { buildFactSignature } from "../../signatures/fact-signature.js";
import { applyPushGates } from "../../gates/push-gates.js";

const dedupeByRuleId = (citations: RuleCitation[]): RuleCitation[] => {
  const seen = new Set<string>();
  const unique: RuleCitation[] = [];
  for (const citation of citations) {
    if (seen.has(citation.ruleId)) continue;
    seen.add(citation.ruleId);
    unique.push(citation);
  }
  return unique;
};

export const evaluatePush = (facts: PushFacts, rules: RuleSet): EvaluationResult => {
  // 규정이 무엇을 요구하는지가 먼저다. 게이트는 이 결과를 지우지 않는다.
  const view = buildPushAccounts(facts, rules);
  const verdict = applyPushGates(facts, rules);

  const { signature, input } = buildFactSignature({
    contactDetected: facts.contactDetected,
    severity: facts.severity,
    opponentDisplacement: facts.opponentDisplacement,
    insidePenaltyArea: facts.insidePenaltyArea,
    cameraSufficiency: facts.cameraSufficiency,
  });

  const citations = dedupeByRuleId([
    ...verdict.citations,
    ...view.narrowedTo,
    ...view.accounts.flatMap((account) => account.citations),
  ]);

  if (citations.length === 0) {
    // 조항 없이 결론만 나가는 경로를 만들지 않는다 (README 11절).
    throw new Error("evaluatePush가 인용 없이 결과를 만들려 했음 — 규칙 데이터를 확인할 것");
  }

  return {
    accounts: view.accounts,
    conflicts: view.conflicts,
    narrowedTo: view.narrowedTo,
    blockedFrom: view.blockedFrom,
    varAssessment: null,
    decision: verdict.decision,
    severity: verdict.severity,
    restart: verdict.restart,
    disciplinary: verdict.disciplinary,
    // 비교할 관측 판정이 아직 입력에 없다. 없는 비교를 MATCH로 채우지 않는다.
    decisionMatch: "UNDETERMINED",
    confidence: verdict.confidence,
    inconclusiveReason: verdict.inconclusiveReason,
    factSignature: signature,
    factSignatureInput: input,
    citations,
  };
};
```

- [x] **Step 4: `index.ts`를 쓴다**

```ts
export { buildPushAccounts } from "./interpreter/build-accounts.js";
export type { PushAccountView } from "./interpreter/build-accounts.js";
export { applyPushGates, disciplinaryFor } from "./gates/push-gates.js";
export type { PushVerdict } from "./gates/push-gates.js";
export { buildFactSignature, FORBIDDEN_SIGNATURE_KEY_PATTERN } from "./signatures/fact-signature.js";
export type { FactSignature } from "./signatures/fact-signature.js";
export { evaluatePush } from "./evaluators/pushing/evaluate-push.js";
```

- [x] **Step 5: 통과를 확인한다**

Run: `npx vitest run packages/rule-engine && npm run typecheck`
Expected: PASS — 21 케이스 (build-accounts 6 + gates 9 + fact-signature 6), evaluate-push 6 추가로 27 케이스

---

### Task 8: `evaluateVar` — 네 게이트를 각각 계산한다

**Files:**
- Create: `packages/rule-engine/src/var/evaluate-var.ts`
- Modify: `packages/rule-engine/src/index.ts` (재수출 추가)
- Test: `packages/rule-engine/test/evaluate-var.test.ts`

**Interfaces:**
- Consumes: Task 2의 `VarFacts` `CompetitionOptions` `VarOutcome` `VarAssessment` `RuleSet`; Task 6의 `buildFactSignature`.
- Produces: `evaluateVar(facts: VarFacts, rules: RuleSet, competitionOptions: CompetitionOptions): VarOutcome`. `index.ts`에 `export { evaluateVar } from "./var/evaluate-var.js";` 추가.

**네 게이트는 전부 계산한다**

게이트 1이 막아도 게이트 2·3·4를 계산해서 채운다. 그래야 "검토 대상이 아니었고, 재개도 이미 됐고, 오심 정도는 미확정" 같은 상태가 한 화면에 나온다. 앞 게이트가 막았다고 뒤 게이트를 비우면 `var-19`가 요구하는 네 필드의 독립성이 무너진다. **선후 관계가 나타나는 곳은 `intervention`·`noInterventionReason`·`reviewProcedure` 세 필드뿐이다.** 앞의 둘은 게이트 1→2→3 순서를 읽고, `reviewProcedure`는 게이트 1이 막으면 `NONE`이 된다 — 검토가 없으면 검토 절차도 없다(`var-02`가 이 값을 고정한다). 나머지 세 게이트 값은 서로를 보지 않는다.

**세 가지 매핑 결정**

1. **채택되지 않은 옵션 범주는 `category`를 `NONE`으로 접지 않는다.** 판본에 그 범주가 있는지(`CORNER_KICK`)와 대회가 쓰는지(`reviewable: false`)는 다른 질문이다. `category: "NONE"`으로 접으면 "규정에 없다"와 "우리 대회는 안 쓴다"가 화면에서 같은 문장이 된다 (README 8절).
2. **`windowException`은 예외 사유를 접지 않는다.** 원문(VAR 1.10 / Law 5.3)의 예외는 선수 확인 오류와 폭력 행위·침 뱉기·물기·심한 모욕 관련 퇴장이다. README 8절 초안의 `MISTAKEN_IDENTITY | VIOLENT_CONDUCT` 두 값으로는 "침 뱉기로 재개 후 퇴장"을 표현할 수 없으므로, `VAR_WINDOW_EXCEPTIONS`를 원문이 나눈 만큼으로 넓혀 `SEND_OFF_CATEGORIES`와 값을 맞췄다. 그래서 예외 판정에 변환표가 없다 — 예외 목록에 걸린 퇴장 사유가 곧 예외 사유다. 목록 자체는 데이터가 소유한다.
3. **문턱이 `UNDETERMINED`면 `noInterventionReason`을 `null`로 둔다.** `THRESHOLD_NOT_MET`은 "문턱에 못 미친다고 확인했다"는 뜻이고 미확정은 확인하지 못한 것이다. 사유를 지어내지 않는다.

- [x] **Step 1: 실패하는 테스트를 쓴다**

`packages/rule-engine/test/evaluate-var.test.ts`:

```ts
import type { VarFacts } from "@replay/shared-types";
import { loadRuleSet } from "@replay/rule-data";
import { describe, expect, it } from "vitest";
import { evaluateVar } from "../src/index.js";

const rules2025 = loadRuleSet("ifab-2025-26")!;

const base: VarFacts = {
  reviewScenario: "PENALTY_NOT_GIVEN",
  restartOccurred: false,
  sendOffCategory: "NONE",
  mistakenIdentity: false,
  decisionNature: "SUBJECTIVE",
  errorMagnitude: "CLEAR_AND_OBVIOUS",
  seriousMissedIncident: false,
};

const assess = (facts: Partial<VarFacts>, options = {}) => {
  const outcome = evaluateVar({ ...base, ...facts }, rules2025, options);
  if (!outcome.ok) throw new Error(`예상치 못한 오류: ${outcome.error}`);
  return outcome.assessment;
};

describe("evaluateVar", () => {
  it("네 게이트가 전부 별도 필드로 나온다", () => {
    const assessment = assess({});
    expect(Object.keys(assessment)).toEqual(
      expect.arrayContaining([
        "category",
        "withinTimeWindow",
        "thresholdMet",
        "reviewProcedure",
      ]),
    );
    expect(assessment.category).toBe("PENALTY_NO_PENALTY");
    expect(assessment.withinTimeWindow).toBe(true);
    expect(assessment.thresholdMet).toBe("MET");
    expect(assessment.reviewProcedure).toBe("OFR");
  });

  it("범주 밖이면 오심 정도와 무관하게 검토 불가다", () => {
    const assessment = assess({ reviewScenario: "OTHER" });
    expect(assessment.reviewable).toBe(false);
    expect(assessment.category).toBe("NONE");
    expect(assessment.notReviewableReason).toBe("OUTSIDE_REVIEWABLE_CATEGORIES");
    expect(assessment.noInterventionReason).toBe("NOT_REVIEWABLE");
    expect(assessment.reviewProcedure).toBe("NONE");
    // 문턱은 여전히 별도로 계산된다
    expect(assessment.thresholdMet).toBe("MET");
  });

  it("주심이 판정을 내렸다는 사실만으로는 검토 창이 닫히지 않는다", () => {
    const assessment = assess({ restartOccurred: false });
    expect(assessment.withinTimeWindow).toBe(true);
    expect(assessment.windowClosedReason).toBeNull();
  });

  it("검토 창을 닫는 것은 재개뿐이다", () => {
    const assessment = assess({ restartOccurred: true });
    expect(assessment.withinTimeWindow).toBe(false);
    expect(assessment.windowClosedReason).toBe("PLAY_RESTARTED");
    expect(assessment.noInterventionReason).toBe("TOO_LATE");
  });

  it("폭력 행위 퇴장은 재개 뒤에도 창이 열려 있다", () => {
    const assessment = assess({
      reviewScenario: "SENDING_OFF_NOT_GIVEN",
      restartOccurred: true,
      sendOffCategory: "VIOLENT_CONDUCT",
    });
    expect(assessment.withinTimeWindow).toBe(true);
    expect(assessment.windowException).toBe("VIOLENT_CONDUCT");
    expect(assessment.windowClosedReason).toBeNull();
  });

  it("검토 대상이면서 개입하지 않은 상태를 만들 수 있다", () => {
    const assessment = assess({ errorMagnitude: "NOT_CLEAR_AND_OBVIOUS" });
    expect(assessment.reviewable).toBe(true);
    expect(assessment.thresholdMet).toBe("NOT_MET");
    expect(assessment.intervention).toBe("NO_INTERVENTION");
    expect(assessment.noInterventionReason).toBe("THRESHOLD_NOT_MET");
  });

  it("문턱이 미확정이면 사유를 지어내지 않는다", () => {
    const assessment = assess({ errorMagnitude: "UNDETERMINED" });
    expect(assessment.thresholdMet).toBe("UNDETERMINED");
    expect(assessment.intervention).toBe("NO_INTERVENTION");
    expect(assessment.noInterventionReason).toBeNull();
  });

  it("절차는 사실적·주관적 구분에서만 나오고 문턱과 무관하다", () => {
    expect(assess({ decisionNature: "FACTUAL" }).reviewProcedure).toBe("VAR_ONLY");
    expect(assess({ decisionNature: "FACTUAL", errorMagnitude: "NOT_CLEAR_AND_OBVIOUS" }).reviewProcedure).toBe(
      "VAR_ONLY",
    );
  });

  it("2025/26에서 2차 경고는 검토 범주가 아니다", () => {
    const assessment = assess({ reviewScenario: "SECOND_CAUTION" });
    expect(assessment.reviewable).toBe(false);
    expect(assessment.notReviewableReason).toBe("OUTSIDE_REVIEWABLE_CATEGORIES");
  });

  it("결과에 인용과 서명이 함께 붙는다", () => {
    const outcome = evaluateVar(base, rules2025, {});
    if (!outcome.ok) throw new Error("예상치 못한 오류");
    expect(outcome.citations.length).toBeGreaterThanOrEqual(1);
    expect(outcome.factSignature).toMatch(/^[0-9a-f]{64}$/);
    expect(outcome.factSignatureInput).not.toContain("competition");
  });
});
```

- [x] **Step 2: 실패를 확인한다**

Run: `npx vitest run packages/rule-engine/test/evaluate-var.test.ts`
Expected: FAIL — `evaluateVar is not a function`

- [x] **Step 3: `evaluate-var.ts`를 쓴다**

```ts
import type {
  CompetitionOptions,
  RuleCitation,
  RuleSet,
  VarAssessment,
  VarCategory,
  VarCategoryRule,
  VarFacts,
  VarNotReviewableReason,
  VarOutcome,
  VarReviewProcedure,
  SendOffCategory,
  VarThresholdResult,
  VarWindowException,
} from "@replay/shared-types";
import { VAR_WINDOW_EXCEPTIONS } from "@replay/shared-types";
import { buildFactSignature } from "../signatures/fact-signature.js";

/**
 * 두 어휘가 겹치는 값인지 확인한다.
 *
 * SEND_OFF_CATEGORIES는 8값, VAR_WINDOW_EXCEPTIONS는 5값이고 겹치는 것은
 * 세 가지 퇴장 사안뿐이다. DOGSO나 SECOND_CAUTION은 퇴장 사유이긴 해도
 * 닫힌 검토 창을 다시 열지 않는다. 캐스팅으로 넘기면 판본 데이터가
 * 그런 값을 예외 목록에 넣었을 때 타입도 검사기도 잡지 못한다.
 */
const isWindowException = (
  value: SendOffCategory,
): value is SendOffCategory & VarWindowException =>
  (VAR_WINDOW_EXCEPTIONS as readonly string[]).includes(value);

type CategoryGate = {
  reviewable: boolean;
  category: VarCategory;
  notReviewableReason: VarNotReviewableReason | null;
};

/** 게이트 1 — 판본 데이터에서 조회한다. 오심의 정도는 여기 관여하지 않는다. */
const categoryGate = (
  matched: VarCategoryRule | undefined,
  competitionOptions: CompetitionOptions,
): CategoryGate | { unknownOption: string } => {
  if (!matched) {
    return {
      reviewable: false,
      category: "NONE",
      notReviewableReason: "OUTSIDE_REVIEWABLE_CATEGORIES",
    };
  }

  if (matched.requiresCompetitionOption !== null) {
    const adopted = competitionOptions[matched.requiresCompetitionOption];
    if (adopted === undefined) {
      // 모르는 것을 미채택으로 접지 않는다
      return { unknownOption: matched.requiresCompetitionOption };
    }
    if (!adopted) {
      // 판본에는 있으나 대회가 쓰지 않는다 — category는 그대로 둔다
      return {
        reviewable: false,
        category: matched.id,
        notReviewableReason: "COMPETITION_OPTION_NOT_ADOPTED",
      };
    }
  }

  return { reviewable: true, category: matched.id, notReviewableReason: null };
};

/**
 * 게이트 3 — 네 범주 전부에 같은 문턱이 적용된다.
 *
 * 원문은 두 갈래를 OR로 잇는다:
 *   "clear and obvious error" **or** "serious missed incident"
 * 그래서 미인지 사건은 errorMagnitude의 값이 아니라 별도 입력이고,
 * 오심 정도가 UNDETERMINED여도 미인지가 확인되면 문턱은 넘는다.
 */
const thresholdGate = (facts: VarFacts): VarThresholdResult => {
  if (facts.seriousMissedIncident) return "MET";

  switch (facts.errorMagnitude) {
    case "CLEAR_AND_OBVIOUS":
      return "MET";
    case "NOT_CLEAR_AND_OBVIOUS":
      return "NOT_MET";
    case "UNDETERMINED":
      return "UNDETERMINED";
  }
};

/** 게이트 4 — 문턱과 무관하게 절차만 가른다. */
const procedureGate = (facts: VarFacts, reviewable: boolean): VarReviewProcedure => {
  if (!reviewable) return "NONE";
  return facts.decisionNature === "SUBJECTIVE" ? "OFR" : "VAR_ONLY";
};

export const evaluateVar = (
  facts: VarFacts,
  rules: RuleSet,
  competitionOptions: CompetitionOptions,
): VarOutcome => {
  // 조건을 만족하는 규칙 중 **더 구체적인 것**이 이긴다.
  //
  // `CARD_SHOWN`은 RED_CARD와 MISTAKEN_IDENTITY 양쪽의 appliesTo에 들어 있고,
  // 다른 선수에게 준 카드였다면 후자가 맞다. 데이터를 IFAB 원문 순서
  // (a 득점 · b PK · c 직접 퇴장 · d 선수 확인 오류)대로 두기 위해
  // 순서가 아니라 조건 수로 고른다 — 데이터 순서를 판정 규칙으로 쓰면
  // 판본 파일의 줄 순서가 결과를 바꾼다.
  const candidates = rules
    .varCategories()
    .filter(
      (category) =>
        category.appliesTo.includes(facts.reviewScenario) &&
        (!category.requiresMistakenIdentity || facts.mistakenIdentity),
    );
  const matched =
    candidates.find((category) => category.requiresMistakenIdentity) ?? candidates[0];

  const gate1 = categoryGate(matched, competitionOptions);
  if ("unknownOption" in gate1) {
    return {
      ok: false,
      error: "UNKNOWN_COMPETITION_OPTION",
      message: `대회 채택 여부가 주어지지 않은 옵션: ${gate1.unknownOption}`,
    };
  }

  // 게이트 2 — 검토 창을 닫는 유일한 조건은 재개다.
  //
  // 원문(VAR 1.10 / Law 5.3)의 예외는 선수 확인 오류와 세 가지 퇴장 사안이다.
  // 어느 예외였는지를 접지 않고 그대로 보고한다 — 화면에서
  // "재개 후에도 왜 검토가 가능한가"에 답하는 것이 이 값이다.
  const exceptions = rules.timeWindowExceptions();
  let windowException: VarWindowException = "NONE";
  if (exceptions.mistakenIdentity && facts.mistakenIdentity) {
    windowException = "MISTAKEN_IDENTITY";
  } else if (exceptions.sendOffCategories.includes(facts.sendOffCategory)) {
    // 예외 목록의 값은 그대로 예외 사유가 된다. 변환표를 두지 않는다.
    // 다만 그건 두 어휘가 겹치는 값일 때만 성립한다. 판본 데이터가 겹치지
    // 않는 값을 목록에 넣으면 엔진이 표현할 수 없는 예외를 요구하는 것이고,
    // 그건 사건의 사실값 문제가 아니라 데이터 오류다 — 인용 없는 결과와
    // 같은 부류이므로 같은 방식으로 던진다. enums.mjs가 먼저 잡는다.
    if (!isWindowException(facts.sendOffCategory)) {
      throw new Error(
        `창을 다시 열지 않는 퇴장 사유가 판본의 예외 목록에 있음: ${facts.sendOffCategory} — 규칙 데이터를 확인할 것`,
      );
    }
    windowException = facts.sendOffCategory;
  }
  const withinTimeWindow = !facts.restartOccurred || windowException !== "NONE";

  const thresholdMet = thresholdGate(facts);
  const reviewProcedure = procedureGate(facts, gate1.reviewable);

  const assessment: VarAssessment = {
    reviewable: gate1.reviewable,
    category: gate1.category,
    withinTimeWindow,
    thresholdMet,
    reviewProcedure,
    intervention: "NO_INTERVENTION",
    noInterventionReason: null,
    notReviewableReason: gate1.notReviewableReason,
    windowClosedReason: withinTimeWindow ? null : "PLAY_RESTARTED",
    windowException,
    explanation: "",
  };

  // 선후 관계가 나타나는 곳은 여기뿐이다. 네 게이트 값 자체는 위에서 이미 전부 채워졌다.
  if (!gate1.reviewable) {
    assessment.noInterventionReason = "NOT_REVIEWABLE";
    assessment.explanation =
      gate1.notReviewableReason === "COMPETITION_OPTION_NOT_ADOPTED"
        ? "판본에는 있는 범주이나 이 대회가 채택하지 않음"
        : "이 판본의 검토 가능 범주에 해당하지 않음";
  } else if (!withinTimeWindow) {
    assessment.noInterventionReason = "TOO_LATE";
    assessment.explanation = "경기가 재개되어 검토 창이 닫힘";
  } else if (thresholdMet === "NOT_MET") {
    assessment.noInterventionReason = "THRESHOLD_NOT_MET";
    assessment.explanation = "검토 대상에는 해당하나 명백하고 분명한 오류로 보기 어려움";
  } else if (thresholdMet === "UNDETERMINED") {
    // 사유를 지어내지 않는다. THRESHOLD_NOT_MET은 확인한 결과이고 이건 확인하지 못한 것이다.
    assessment.explanation = "문턱 판단에 필요한 사실값이 확정되지 않아 개입 여부를 말할 수 없음";
  } else {
    assessment.intervention = "OVERTURNED";
    assessment.explanation = "명백하고 분명한 오류에 해당해 원심이 변경될 사안";
  }

  const citations: RuleCitation[] = [
    ...rules.cite("VAR_REVIEWABLE_CATEGORIES"),
    ...rules.cite("VAR_TIME_WINDOW"),
    ...rules.cite("VAR_THRESHOLD"),
    ...rules.cite("VAR_REVIEW_PROCESS"),
  ];

  if (citations.length === 0) {
    throw new Error("evaluateVar가 인용 없이 결과를 만들려 했음 — 규칙 데이터를 확인할 것");
  }

  // 대회 채택 옵션은 사건의 사실값이 아니므로 서명에 넣지 않는다.
  const { signature, input } = buildFactSignature({
    reviewScenario: facts.reviewScenario,
    restartOccurred: facts.restartOccurred,
    sendOffCategory: facts.sendOffCategory,
    mistakenIdentity: facts.mistakenIdentity,
    decisionNature: facts.decisionNature,
    errorMagnitude: facts.errorMagnitude,
    seriousMissedIncident: facts.seriousMissedIncident,
  });

  return { ok: true, assessment, citations, factSignature: signature, factSignatureInput: input };
};
```

- [x] **Step 4: `index.ts`에 재수출을 추가한다**

`packages/rule-engine/src/index.ts` 끝에 한 줄 추가:

```ts
export { evaluateVar } from "./var/evaluate-var.js";
```

- [x] **Step 5: 통과를 확인한다**

Run: `npx vitest run packages/rule-engine && npm run typecheck`
Expected: PASS — evaluate-var 10 케이스 포함 전부 통과

---

### Task 9: 합성 픽스처 27건과 불변식 4건

**Files:**
- Create: `packages/rule-engine/fixtures/push-decision.fixtures.json`
- Create: `packages/rule-engine/fixtures/var-assessment.fixtures.json`

**Interfaces:**
- Consumes: Task 1의 어휘값, Task 2의 필드명.
- Produces: Task 10의 러너가 읽는 두 파일. 스위트 구조는 `{ suite, defaults, baseline, cases[], invariants[] }`이며 **`cases[]`와 `invariants[]`는 별도 배열이다.** 한 배열에는 같은 것만 들어간다 (README 13절).

**케이스는 최소 입력만 쓴다.** `baseline`에 기본 사실값을 두고 `given.facts`에는 그 케이스가 주장하는 것만 덮어쓴다. 그래야 케이스가 깨졌을 때 어느 값이 원인인지 바로 보인다. baseline은 러너 코드가 아니라 픽스처 파일이 소유한다.

**`expect`는 부분 일치다.** 적힌 필드만 비교한다. 전부 적으면 케이스 하나가 여러 가지를 주장하게 되고, 무관한 필드가 바뀔 때 같이 깨진다.

- [x] **Step 1: `push-decision.fixtures.json`을 쓴다**

```json
{
  "suite": "push-decision",
  "defaults": { "ruleVersion": "ifab-2025-26" },
  "baseline": {
    "contactDetected": { "value": true, "observedAtSpeed": "NORMAL" },
    "severity": { "value": "RECKLESS", "observedAtSpeed": "NORMAL" },
    "opponentDisplacement": { "value": "clear", "observedAtSpeed": "NORMAL" },
    "insidePenaltyArea": { "value": false, "observedAtSpeed": "NORMAL" },
    "cameraSufficiency": "HIGH"
  },
  "cases": [
    {
      "id": "push-01",
      "gate": "CAMERA",
      "concern": "각도가 부족하면 다른 사실값이 아무리 선명해도 판정하지 않는다",
      "note": "각도 게이트가 다른 게이트보다 먼저 걸리지 않게 되면, 근거가 부족한 상태에서 강도 판정이 나간다. push-02와 cameraSufficiency 하나만 다르다.",
      "given": { "facts": { "cameraSufficiency": "LOW" } },
      "expect": {
        "decision": "INCONCLUSIVE",
        "inconclusiveReason": "CAMERA_INSUFFICIENT",
        "severity": null,
        "confidence": "LOW"
      }
    },
    {
      "id": "push-02",
      "gate": "CAMERA",
      "concern": "각도가 충분하면 같은 입력이 판정까지 간다",
      "note": "push-01의 짝. 이 케이스가 함께 깨지면 원인은 각도 게이트가 아니라 적용표에 있다.",
      "given": { "facts": { "cameraSufficiency": "MEDIUM" } },
      "expect": {
        "decision": "FOUL",
        "severity": "RECKLESS",
        "restart": "DIRECT_FREE_KICK",
        "disciplinary": "CAUTION",
        "inconclusiveReason": null
      }
    },
    {
      "id": "push-03",
      "gate": "SPEED",
      "concern": "슬로우모션에서만 본 강도는 판정 입력으로 승인하지 않는다",
      "note": "하이라이트에는 리플레이만 있고 라이브 컷이 없는 장면이 실제로 있다. 이게 자주 걸리는 것은 결함이 아니라 의도한 동작이다. push-04와 결과는 같고 사유가 달라야 한다.",
      "given": { "facts": { "severity": { "value": "EXCESSIVE_FORCE", "observedAtSpeed": "SLOW" } } },
      "expect": {
        "decision": "INCONCLUSIVE",
        "inconclusiveReason": "SLOW_MOTION_ONLY",
        "disciplinary": null
      }
    },
    {
      "id": "push-04",
      "gate": "SEVERITY",
      "concern": "정상 속도로 봤지만 강도를 특정 못한 것은 속도 게이트와 다른 사유다",
      "note": "push-03과 결과가 같다. 두 사유를 하나로 뭉개면 사용자가 다음에 무엇을 해야 하는지가 사라진다 — 앞은 라이브 컷을 찾으면 풀리고 뒤는 더 나은 앵글이 필요하다.",
      "given": { "facts": { "severity": { "value": "uncertain", "observedAtSpeed": "NORMAL" } } },
      "expect": {
        "decision": "INCONCLUSIVE",
        "inconclusiveReason": "SEVERITY_UNDETERMINED"
      }
    },
    {
      "id": "push-05",
      "gate": "CONTACT",
      "concern": "접촉이 없으면 강도를 묻지 않고 반칙이 아니다",
      "note": "접촉 확인에까지 속도 게이트를 걸면 규정에 없는 제약이 생긴다. 원문은 슬로우모션을 point of contact에 쓰라고 적고 있다. 그래서 관측 속도를 SLOW로 두고도 통과해야 한다.",
      "given": {
        "facts": {
          "contactDetected": { "value": false, "observedAtSpeed": "SLOW" },
          "severity": { "value": "uncertain", "observedAtSpeed": "SLOW" }
        }
      },
      "expect": { "decision": "NO_FOUL", "restart": "PLAY_CONTINUED", "inconclusiveReason": null }
    },
    {
      "id": "push-06",
      "gate": "SEVERITY",
      "concern": "밀림 없는 부주의 접촉은 반칙이 아니라 정상적인 몸싸움이다",
      "note": "접촉이 있으면 곧 반칙이라는 결론이 나오면 이 케이스가 깨진다. Law 12는 접촉 유무가 아니라 부주의·무모·과도한 힘으로 가른다.",
      "given": {
        "facts": {
          "severity": { "value": "CARELESS", "observedAtSpeed": "NORMAL" },
          "opponentDisplacement": { "value": "none", "observedAtSpeed": "NORMAL" }
        }
      },
      "expect": { "decision": "NORMAL_CONTACT", "restart": "PLAY_CONTINUED", "disciplinary": "NONE" }
    },
    {
      "id": "push-07",
      "gate": "SEVERITY",
      "concern": "possible은 파울이 아니라 보류다",
      "note": "'있었을 수도 있다'를 파울 쪽으로 접으면 관측하지 않은 것을 관측한 것처럼 답하게 된다. 베이스라인과 displacement 하나만 다르다 (push-06은 severity도 함께 덮으므로 단일값 대조 상대가 아니다).",
      "given": { "facts": { "opponentDisplacement": { "value": "possible", "observedAtSpeed": "NORMAL" } } },
      "expect": { "decision": "INCONCLUSIVE", "inconclusiveReason": "SEVERITY_UNDETERMINED" }
    },
    {
      "id": "push-08",
      "gate": "APPLY",
      "concern": "재개는 지역이 정하고 징계는 강도가 정한다",
      "note": "두 값이 한 곳에서 결정되면 페널티지역 안의 부주의 반칙에 퇴장이 붙는 식의 오류가 생긴다. 베이스라인과 insidePenaltyArea·severity가 다르다 (push-02는 cameraSufficiency까지 다르므로 대조 상대가 아니다).",
      "given": {
        "facts": {
          "insidePenaltyArea": { "value": true, "observedAtSpeed": "NORMAL" },
          "severity": { "value": "EXCESSIVE_FORCE", "observedAtSpeed": "NORMAL" }
        }
      },
      "expect": {
        "decision": "FOUL",
        "severity": "EXCESSIVE_FORCE",
        "restart": "PENALTY_KICK",
        "disciplinary": "SEND_OFF"
      }
    }
  ],
  "invariants": [
    {
      "id": "push-09",
      "check": "EVERY_RESULT_HAS_CITATION",
      "appliesTo": "ALL_CASES",
      "note": "조항 없이 결론만 반환하는 경로가 생기면 결과 화면이 근거 없는 판정을 표시하게 된다. 이 스위트에 어떤 케이스를 추가해도 이 검사는 면제되지 않는다."
    },
    {
      "id": "push-10",
      "check": "ACCOUNTS_SURVIVE_BLOCKED_FACTS",
      "appliesTo": ["push-01", "push-03", "push-04", "push-05", "push-07"],
      "expectedAppliesCount": 5,
      "note": "규정이 무엇을 요구하는지는 영상과 무관하게 말할 수 있다. 게이트에서 바로 반환하면 accounts가 빈 채로 나가고, 사용자는 '판정 불가'만 보게 된다. expectedAppliesCount를 둔 이유는 막힌 케이스 집합이 조용히 줄어드는 것을 막기 위함이다. push-05가 목록에 있는 이유: 결론이 NO_FOUL로 확정돼도 서지 않은 사실은 그대로 보고한다 — 막힌 사실 보고를 결론이 흐린 경우로만 좁히지 않는다."
    }
  ]
}
```

- [x] **Step 2: `var-assessment.fixtures.json`을 쓴다**

```json
{
  "suite": "var-assessment",
  "defaults": { "ruleVersion": "ifab-2025-26", "competitionOptions": {} },
  "baseline": {
    "reviewScenario": "PENALTY_NOT_GIVEN",
    "restartOccurred": false,
    "sendOffCategory": "NONE",
    "mistakenIdentity": false,
    "seriousMissedIncident": false,
    "decisionNature": "SUBJECTIVE",
    "errorMagnitude": "CLEAR_AND_OBVIOUS"
  },
  "cases": [
    {
      "id": "var-01",
      "gate": "CATEGORY",
      "concern": "검토 가능 범주에 해당하면 네 게이트가 모두 열린 상태로 나온다",
      "note": "이 스위트의 기준선. 나머지 케이스는 여기서 값 하나씩만 뒤집는다.",
      "given": { "facts": {} },
      "expect": {
        "reviewable": true,
        "category": "PENALTY_NO_PENALTY",
        "withinTimeWindow": true,
        "thresholdMet": "MET",
        "reviewProcedure": "OFR",
        "intervention": "OVERTURNED",
        "noInterventionReason": null
      }
    },
    {
      "id": "var-02",
      "gate": "CATEGORY",
      "concern": "범주 밖이면 오심의 정도와 무관하게 검토 불가다",
      "note": "범주 게이트를 건너뛰면 '명백한 오심인데 VAR가 왜 개입 안 했나'라는 잘못된 출력이 나온다.",
      "given": { "facts": { "reviewScenario": "OTHER" } },
      "expect": {
        "reviewable": false,
        "category": "NONE",
        "notReviewableReason": "OUTSIDE_REVIEWABLE_CATEGORIES",
        "noInterventionReason": "NOT_REVIEWABLE",
        "reviewProcedure": "NONE"
      }
    },
    {
      "id": "var-03",
      "gate": "TIME_WINDOW",
      "concern": "재개되면 검토 창이 닫힌다",
      "note": "var-04와 restartOccurred가 같고 sendOffCategory만 다르다. 두 결과가 같아지면 예외 목록이 읽히지 않고 있는 것이다.",
      "given": {
        "facts": {
          "reviewScenario": "SENDING_OFF_NOT_GIVEN",
          "restartOccurred": true,
          "sendOffCategory": "SERIOUS_FOUL_PLAY"
        }
      },
      "expect": {
        "withinTimeWindow": false,
        "windowClosedReason": "PLAY_RESTARTED",
        "windowException": "NONE",
        "noInterventionReason": "TOO_LATE"
      }
    },
    {
      "id": "var-04",
      "gate": "TIME_WINDOW",
      "concern": "폭력 행위 관련 퇴장은 재개 뒤에도 창이 열려 있다",
      "note": "var-03의 짝. windowException이 NONE이 아니면 재개 여부와 무관하게 withinTimeWindow가 참이어야 한다.",
      "given": {
        "facts": {
          "reviewScenario": "SENDING_OFF_NOT_GIVEN",
          "restartOccurred": true,
          "sendOffCategory": "VIOLENT_CONDUCT"
        }
      },
      "expect": {
        "withinTimeWindow": true,
        "windowClosedReason": null,
        "windowException": "VIOLENT_CONDUCT"
      }
    },
    {
      "id": "var-05",
      "gate": "TIME_WINDOW",
      "concern": "선수 확인 오류도 재개 뒤 예외다",
      "note": "예외가 퇴장 사유 목록에만 걸리면 선수 확인 오류가 빠진다. 두 예외 경로가 서로 다른 필드에서 온다는 것을 고정한다. 선수 확인 오류를 reviewScenario 값으로 두지 않는 이유: 그것은 사건의 종류가 아니라 카드 수여자의 속성이고, 사건은 여전히 '카드가 나왔다'이다.",
      "given": { "facts": { "reviewScenario": "CARD_SHOWN", "restartOccurred": true, "mistakenIdentity": true } },
      "expect": {
        "withinTimeWindow": true,
        "windowException": "MISTAKEN_IDENTITY",
        "category": "MISTAKEN_IDENTITY"
      }
    },
    {
      "id": "var-06",
      "gate": "TIME_WINDOW",
      "concern": "주심이 이미 판정을 내렸다는 사실은 검토를 막지 않는다",
      "note": "'원심을 선언했으니 VAR 검토가 불가하다'는 규정을 뒤집은 진술이다. 엔진은 이 형태의 결론을 낼 수 없어야 한다. 검토 창을 닫는 유일한 조건은 재개다.",
      "given": { "facts": { "reviewScenario": "PENALTY_GIVEN", "restartOccurred": false } },
      "expect": { "reviewable": true, "withinTimeWindow": true, "windowClosedReason": null }
    },
    {
      "id": "var-07",
      "gate": "THRESHOLD",
      "concern": "속행 판정도 심각한 미인지 사건으로 검토 대상이 된다",
      "note": "'판정을 내리지 않았다'를 검토 불가로 접으면 미인지 사건 경로가 통째로 사라진다.",
      "given": { "facts": { "seriousMissedIncident": true, "errorMagnitude": "UNDETERMINED" } },
      "expect": { "reviewable": true, "thresholdMet": "MET", "intervention": "OVERTURNED" }
    },
    {
      "id": "var-08",
      "gate": "CATEGORY",
      "concern": "득점 인정은 득점 범주다",
      "note": "var-09와 함께 한 범주가 두 사건 유형을 덮는다는 것을 고정한다.",
      "given": { "facts": { "reviewScenario": "GOAL_AWARDED" } },
      "expect": { "reviewable": true, "category": "GOAL_NO_GOAL" }
    },
    {
      "id": "var-09",
      "gate": "CATEGORY",
      "concern": "득점 취소도 같은 득점 범주다",
      "note": "var-08의 짝. 둘이 다른 범주로 갈리면 범주 매핑이 사건 결과를 보고 있는 것이다.",
      "given": { "facts": { "reviewScenario": "GOAL_DISALLOWED" } },
      "expect": { "reviewable": true, "category": "GOAL_NO_GOAL" }
    },
    {
      "id": "var-10",
      "gate": "PROCEDURE",
      "concern": "주관적 판정은 온필드 리뷰다",
      "note": "var-11과 decisionNature가 같고 errorMagnitude만 다르다. 절차는 같고 문턱만 뒤집혀야 한다.",
      "given": { "facts": { "decisionNature": "SUBJECTIVE", "errorMagnitude": "CLEAR_AND_OBVIOUS" } },
      "expect": { "reviewProcedure": "OFR", "thresholdMet": "MET" }
    },
    {
      "id": "var-11",
      "gate": "PROCEDURE",
      "concern": "문턱이 바뀌어도 절차는 바뀌지 않는다",
      "note": "var-10의 짝. 절차가 함께 바뀌면 문턱 게이트와 절차 게이트가 한 값에서 나오고 있는 것이다.",
      "given": { "facts": { "decisionNature": "SUBJECTIVE", "errorMagnitude": "NOT_CLEAR_AND_OBVIOUS" } },
      "expect": {
        "reviewProcedure": "OFR",
        "thresholdMet": "NOT_MET",
        "reviewable": true,
        "intervention": "NO_INTERVENTION",
        "noInterventionReason": "THRESHOLD_NOT_MET"
      }
    },
    {
      "id": "var-12",
      "gate": "CATEGORY",
      "concern": "2025/26에서 2차 경고는 검토 범주가 아니다",
      "note": "var-13과 입력이 같고 rule_version만 다르다. 두 결과가 같아지면 범주 목록이 데이터가 아니라 코드에 있는 것이다.",
      "given": { "ruleVersion": "ifab-2025-26", "facts": { "reviewScenario": "SECOND_CAUTION" } },
      "expect": {
        "reviewable": false,
        "category": "NONE",
        "notReviewableReason": "OUTSIDE_REVIEWABLE_CATEGORIES"
      }
    },
    {
      "id": "var-13",
      "gate": "CATEGORY",
      "concern": "2026/27에서 2차 경고는 퇴장 범주에 들어간다",
      "note": "var-12의 짝. 판본이 결과를 바꾼다는 것을 고정한다.",
      "given": { "ruleVersion": "ifab-2026-27", "facts": { "reviewScenario": "SECOND_CAUTION" } },
      "expect": { "reviewable": true, "category": "RED_CARD", "notReviewableReason": null }
    },
    {
      "id": "var-14",
      "gate": "CATEGORY",
      "concern": "판본이 열어둔 범주라도 대회가 채택하지 않으면 검토 대상이 아니다",
      "note": "category를 NONE으로 접지 않는다. '규정에 없다'와 '우리 대회는 안 쓴다'를 같은 문장으로 답하면 3계층 구조가 화면에서 사라진다.",
      "given": {
        "ruleVersion": "ifab-2026-27",
        "competitionOptions": { "corner_kick_review": false },
        "facts": { "reviewScenario": "CORNER_KICK_AWARDED" }
      },
      "expect": {
        "reviewable": false,
        "category": "CORNER_KICK",
        "notReviewableReason": "COMPETITION_OPTION_NOT_ADOPTED",
        "noInterventionReason": "NOT_REVIEWABLE"
      }
    },
    {
      "id": "var-15",
      "gate": "CATEGORY",
      "concern": "대회가 채택하면 같은 판본에서 검토 대상이 된다",
      "note": "var-14의 짝. 판본은 같고 채택 옵션만 다르다. 옵션이 판본과 별개 축임을 고정한다.",
      "given": {
        "ruleVersion": "ifab-2026-27",
        "competitionOptions": { "corner_kick_review": true },
        "facts": { "reviewScenario": "CORNER_KICK_AWARDED" }
      },
      "expect": { "reviewable": true, "category": "CORNER_KICK", "notReviewableReason": null }
    },
    {
      "id": "var-16",
      "gate": "PROCEDURE",
      "concern": "사실적 판정은 VAR-only 리뷰다",
      "note": "사실적·주관적 구분은 문턱을 가르지 않고 절차만 가른다. var-10과 결과 절차가 달라야 한다.",
      "given": { "facts": { "reviewScenario": "GOAL_DISALLOWED", "decisionNature": "FACTUAL" } },
      "expect": { "reviewProcedure": "VAR_ONLY", "thresholdMet": "MET" }
    },
    {
      "id": "var-17",
      "gate": "THRESHOLD",
      "concern": "문턱을 판단할 수 없으면 미달로 접지 않는다",
      "note": "문턱 판단은 강도에 의존하고 강도가 속도 게이트에 걸리면 UNDETERMINED가 정직한 값이다. 여기서 THRESHOLD_NOT_MET이 나오면 확인하지 않은 것을 확인한 것처럼 답하는 것이다.",
      "given": { "facts": { "errorMagnitude": "UNDETERMINED" } },
      "expect": {
        "reviewable": true,
        "thresholdMet": "UNDETERMINED",
        "intervention": "NO_INTERVENTION",
        "noInterventionReason": null
      }
    },
    {
      "id": "var-18",
      "gate": "CATEGORY",
      "concern": "문턱을 넘어도 범주 밖이면 개입 사유가 NOT_REVIEWABLE이어야 한다",
      "note": "정답을 내면서 이유를 틀리는 경우가 여기서 잡힌다. 개입하지 않은 결과는 var-11과 같지만 이유가 다르다. 두 게이트를 합치면 이 구분이 사라진다. OTHER는 어느 범주에도 닿지 않는 관측을 받는 값이다 — 스로인 판정이나 오프사이드 위치 단독 장면이 여기로 들어온다.",
      "given": { "facts": { "reviewScenario": "OTHER", "errorMagnitude": "CLEAR_AND_OBVIOUS" } },
      "expect": {
        "reviewable": false,
        "thresholdMet": "MET",
        "intervention": "NO_INTERVENTION",
        "noInterventionReason": "NOT_REVIEWABLE",
        "notReviewableReason": "OUTSIDE_REVIEWABLE_CATEGORIES"
      }
    },
    {
      "id": "var-21",
      "gate": "CATEGORY",
      "concern": "채택 여부를 모르면 미채택으로 추정하지 않고 오류를 낸다",
      "note": "var-14와 짝이다. K리그의 실제 코너킥 검토 채택 여부는 아직 미확인이다. 값 없음을 false로 접으면 확인하지 않은 것이 확인된 것으로 바뀐다.",
      "given": {
        "ruleVersion": "ifab-2026-27",
        "competitionOptions": {},
        "facts": { "reviewScenario": "CORNER_KICK_AWARDED" }
      },
      "expect": { "error": "UNKNOWN_COMPETITION_OPTION" }
    }
  ],
  "invariants": [
    {
      "id": "var-19",
      "check": "FOUR_GATES_SEPARATE",
      "appliesTo": "ALL_SUCCESSFUL_CASES",
      "note": "이 스위트 전체의 핵심 기준. 네 값이 하나로 접힌 결론만 돌아오면 개별 케이스가 통과해도 여기서 차단된다. 검사는 세 가지다 — 네 필드가 각각 존재할 것, reviewable이 true이면서 개입하지 않은 결과가 최소 하나 있을 것, 범주 밖이면서 문턱을 넘은 결과가 최소 하나 있을 것."
    },
    {
      "id": "var-20",
      "check": "FACT_SIGNATURE_SAFE_AND_DETERMINISTIC",
      "appliesTo": "ALL_SUCCESSFUL_CASES",
      "note": "서명은 사건이 아니라 사실 조합을 가리켜야 한다. 경기·심판 식별자가 섞이면 서명이 개인을 추적하는 키가 되고, 결정적이지 않으면 재현 검증에 쓸 수 없다. 서로 다른 입력이 같은 서명을 갖는 것도 실패다."
    }
  ]
}
```

- [x] **Step 3: JSON 문법과 케이스 수를 확인한다**

Run:

```bash
node -e '
const fs = require("node:fs");
for (const name of ["push-decision", "var-assessment"]) {
  const suite = JSON.parse(fs.readFileSync(`packages/rule-engine/fixtures/${name}.fixtures.json`, "utf8"));
  const ids = suite.cases.map((entry) => entry.id);
  console.log(name, "cases:", ids.length, "invariants:", suite.invariants.length);
  if (new Set(ids).size !== ids.length) throw new Error("중복 id");
}'
```

Expected:

```text
push-decision cases: 8 invariants: 2
var-assessment cases: 19 invariants: 2
```

---

### Task 10: 2026/27 판본 데이터

**Files:**
- Create: `packages/rule-data/data/ifab/2026-27.json`
- Modify: `packages/rule-data/src/load-rule-set.ts` (`FILES` 배열에 추가)
- Modify: `packages/rule-data/test/load-rule-set.test.ts` (판본 비교 케이스 추가)

**Interfaces:**
- Consumes: Task 3의 `RuleSetFile` `loadRuleSet`.
- Produces: `loadRuleSet("ifab-2026-27")`가 5개 범주를 반환하며, `RED_CARD.appliesTo`에 `SECOND_CAUTION`이 포함되고 `CORNER_KICK.requiresCompetitionOption === "corner_kick_review"`다.

**두 판본의 차이는 두 종류다.** 2차 경고는 **기존 범주의 적용 대상이 넓어진 것**이고 코너킥은 **범주가 새로 생긴 것**이다. 전자를 새 범주로 만들면 화면에 없는 범주가 하나 더 생기고, 후자를 적용 대상 확장으로 만들면 대회 채택 옵션을 붙일 자리가 없어진다. 데이터 구조가 이 차이를 그대로 담는지가 이 태스크의 요점이다.

- [x] **Step 1: 실패하는 테스트를 먼저 추가한다**

`packages/rule-data/test/load-rule-set.test.ts` 끝에 붙인다:

```ts
describe("판본 차이", () => {
  const ruleSet2026 = loadRuleSet("ifab-2026-27");

  it("2026/27은 다섯 범주이고 코너킥에 대회 채택 옵션이 붙어 있다", () => {
    const categories = ruleSet2026!.varCategories();
    expect(categories.map((category) => category.id)).toEqual([
      "GOAL_NO_GOAL",
      "PENALTY_NO_PENALTY",
      "RED_CARD",
      "MISTAKEN_IDENTITY",
      "CORNER_KICK",
    ]);
    const corner = categories.find((category) => category.id === "CORNER_KICK");
    expect(corner?.requiresCompetitionOption).toBe("corner_kick_review");
    expect(corner?.appliesTo).toEqual(["CORNER_KICK_AWARDED"]);
  });

  it("2차 경고는 새 범주가 아니라 퇴장 범주의 적용 대상 확장이다", () => {
    const redCard2026 = ruleSet2026!.varCategories().find((category) => category.id === "RED_CARD");
    expect(redCard2026?.appliesTo).toEqual(["CARD_SHOWN", "SENDING_OFF_NOT_GIVEN", "SECOND_CAUTION"]);
    expect(redCard2026?.requiresCompetitionOption).toBeNull();
  });

  it("두 판본의 인용은 판본 문자열까지 서로 다르다", () => {
    const older = ruleSet2025!.cite("LAW_12_DIRECT_FREE_KICK")[0]!;
    const newer = ruleSet2026!.cite("LAW_12_DIRECT_FREE_KICK")[0]!;
    expect(older.edition).toBe("2025-26");
    expect(newer.edition).toBe("2026-27");
    expect(older.ruleId).not.toBe(newer.ruleId);
    expect(older.sourcePage).toBe("109");
    expect(newer.sourcePage).toBe("115");
  });

  it("재개 예외는 두 판본에서 같다", () => {
    expect(ruleSet2026!.timeWindowExceptions()).toEqual(ruleSet2025!.timeWindowExceptions());
  });
});
```

- [x] **Step 2: 실패를 확인한다**

Run: `npx vitest run packages/rule-data`
Expected: FAIL — `ruleSet2026` is null

- [x] **Step 3: `2026-27.json`을 쓴다**

인용문은 `rules/ifab/2026-27/Laws of the Game 2026_27_single pages.pdf`에서 발췌했고 쪽번호는 그 PDF의 인쇄 쪽번호다.

```json
{
  "versionId": "ifab-2026-27",
  "authority": "IFAB",
  "edition": "2026-27",
  "concepts": {
    "LAW_12_DIRECT_FREE_KICK": [
      {
        "key": "law-12-1-direct-free-kick",
        "revision": 1,
        "law": "12",
        "section": "1",
        "relevance": "PRIMARY",
        "sourcePage": "115",
        "quoteSnapshot": "A direct free kick is awarded if a player commits any of the following offences against an opponent in a manner considered by the referee to be careless, reckless or using excessive force: charges; jumps at; kicks or attempts to kick; pushes; strikes or attempts to strike (including head-butt); tackles or challenges; trips or attempts to trip. If an offence involves contact, it is penalised by a direct free kick."
      }
    ],
    "LAW_12_DISCIPLINE": [
      {
        "key": "law-12-1-discipline",
        "revision": 1,
        "law": "12",
        "section": "1",
        "relevance": "PRIMARY",
        "sourcePage": "115",
        "quoteSnapshot": "Careless is when a player shows a lack of attention or consideration when making a challenge or acts without precaution. No disciplinary sanction is needed. Reckless is when a player acts with disregard to the danger to, or consequences for, an opponent and must be cautioned. Using excessive force is when a player exceeds the necessary use of force and/or endangers the safety of an opponent and must be sent off."
      }
    ],
    "VAR_REVIEWABLE_CATEGORIES": [
      {
        "key": "var-1-1-categories",
        "revision": 1,
        "law": "VAR",
        "section": "1.1",
        "relevance": "PRIMARY",
        "sourcePage": "153",
        "quoteSnapshot": "A video assistant referee (VAR) is a match official, with independent access to match footage, who may assist the referee only in the event of a 'clear and obvious error' or 'serious missed incident' in relation to: a. Goal/no goal b. Penalty/no penalty c. Red card (including a clearly incorrect second caution) d. Mistaken identity (when the referee cautions or sends off the wrong player)"
      },
      {
        "key": "var-2-c-red-cards",
        "revision": 1,
        "law": "VAR",
        "section": "2.c",
        "relevance": "PRIMARY",
        "sourcePage": "155",
        "quoteSnapshot": "Red cards: DOGSO (especially position of offence and positions of other players); serious foul play (or reckless challenge); violent conduct, biting or spitting at another person; using offensive, insulting or abusive action(s); clearly incorrect second caution"
      },
      {
        "key": "var-2-e-corner-kick",
        "revision": 1,
        "law": "VAR",
        "section": "2.e",
        "relevance": "PRIMARY",
        "sourcePage": "155",
        "quoteSnapshot": "Clearly incorrectly awarded corner kick if the decision can be changed immediately and without delaying the restart (competition option)"
      }
    ],
    "VAR_TIME_WINDOW": [
      {
        "key": "var-1-10-restart-closes-window",
        "revision": 1,
        "law": "VAR",
        "section": "1.10",
        "relevance": "PRIMARY",
        "sourcePage": "154",
        "quoteSnapshot": "If play has stopped and been restarted, the referee may not undertake a 'review' except for a case of mistaken identity or for a potential sending-off offence relating to violent conduct, spitting, biting or extremely offensive, insulting and/or abusive action(s)."
      },
      {
        "key": "var-1-2-original-decision-required",
        "revision": 1,
        "law": "VAR",
        "section": "1.2",
        "relevance": "SUPPORTING",
        "sourcePage": "153",
        "quoteSnapshot": "The referee must always make a decision, i.e. the referee is not permitted to give 'no decision' and then use the VAR to make the decision; a decision to allow play to continue after an alleged offence can be reviewed."
      }
    ],
    "VAR_THRESHOLD": [
      {
        "key": "var-1-3-clear-and-obvious",
        "revision": 1,
        "law": "VAR",
        "section": "1.3",
        "relevance": "PRIMARY",
        "sourcePage": "153",
        "quoteSnapshot": "The original decision given by the referee will not be changed unless the video review clearly shows that the decision was a 'clear and obvious error'."
      }
    ],
    "VAR_REVIEW_PROCESS": [
      {
        "key": "var-3-replay-speed",
        "revision": 1,
        "law": "VAR",
        "section": "3",
        "relevance": "PRIMARY",
        "sourcePage": "157",
        "quoteSnapshot": "The VAR can 'check' the footage in normal speed and/or in slow motion but, in general, slow-motion replays should only be used for facts, e.g. position of offence/player, point of contact for physical offences and handball, ball out of play (including goal/no goal); normal speed should be used for the 'intensity' of an offence or to decide if it was a handball offence"
      },
      {
        "key": "var-1-5-final-decision-ofr",
        "revision": 1,
        "law": "VAR",
        "section": "1.5",
        "relevance": "SUPPORTING",
        "sourcePage": "154",
        "quoteSnapshot": "The final decision is always taken by the referee, either based on information from the VAR or after the referee has undertaken an 'on-field review' (OFR)."
      }
    ]
  },
  "varCategories": [
    {
      "id": "GOAL_NO_GOAL",
      "appliesTo": ["GOAL_AWARDED", "GOAL_DISALLOWED"],
      "requiresCompetitionOption": null,
      "requiresMistakenIdentity": false
    },
    {
      "id": "PENALTY_NO_PENALTY",
      "appliesTo": ["PENALTY_GIVEN", "PENALTY_NOT_GIVEN"],
      "requiresCompetitionOption": null,
      "requiresMistakenIdentity": false
    },
    {
      "id": "RED_CARD",
      "appliesTo": ["CARD_SHOWN", "SENDING_OFF_NOT_GIVEN", "SECOND_CAUTION"],
      "requiresCompetitionOption": null,
      "requiresMistakenIdentity": false
    },
    {
      "id": "MISTAKEN_IDENTITY",
      "appliesTo": ["CARD_SHOWN", "SECOND_CAUTION", "SENDING_OFF_NOT_GIVEN"],
      "requiresCompetitionOption": null,
      "requiresMistakenIdentity": true
    },
    {
      "id": "CORNER_KICK",
      "appliesTo": ["CORNER_KICK_AWARDED"],
      "requiresCompetitionOption": "corner_kick_review",
      "requiresMistakenIdentity": false
    }
  ],
  "timeWindowExceptions": {
    "mistakenIdentity": true,
    "sendOffCategories": ["VIOLENT_CONDUCT", "BITING_OR_SPITTING", "OFFENSIVE_LANGUAGE_OR_ACTION"]
  },
  "layerConflicts": []
}
```

- [x] **Step 4: `load-rule-set.ts`에 판본을 등록한다**

import 줄 아래에 한 줄을 더하고 `FILES`를 고친다:

```ts
import ifab202526 from "../data/ifab/2025-26.json" with { type: "json" };
import ifab202627 from "../data/ifab/2026-27.json" with { type: "json" };

const FILES: readonly RuleSetFile[] = [ifab202526 as RuleSetFile, ifab202627 as RuleSetFile];
```

- [x] **Step 5: 통과를 확인한다**

Run: `npx vitest run packages/rule-data && npm run typecheck`
Expected: PASS — 10 케이스

---

### Task 11: 픽스처 러너

**Files:**
- Create: `packages/rule-engine/test/fixtures.test.ts`

**Interfaces:**
- Consumes: Task 7의 `evaluatePush`, Task 8의 `evaluateVar`, Task 6의 `FORBIDDEN_SIGNATURE_KEY_PATTERN`, Task 3·10의 `loadRuleSet`, Task 9의 두 픽스처 파일.
- Produces: 없음 (테스트 파일). 27 케이스 + 4 불변식이 여기서 실행된다. **이 파일이 통과하는 것이 1단계의 완료 기준이다.**

러너는 픽스처의 형태를 되묻지 않는다. `cases[]`의 원소는 전부 `given.facts`를 갖고 `invariants[]`의 원소는 전부 `check`를 갖는다. 그래서 두 배열을 각각 한 가지 방식으로만 처리한다.

- [x] **Step 1: 러너를 쓴다**

`packages/rule-engine/test/fixtures.test.ts`:

```ts
import type {
  CompetitionOptions,
  EvaluationResult,
  PushFacts,
  VarAssessment,
  VarFacts,
} from "@replay/shared-types";
import { loadRuleSet } from "@replay/rule-data";
import { beforeAll, describe, expect, it } from "vitest";
import pushSuite from "../fixtures/push-decision.fixtures.json" with { type: "json" };
import varSuite from "../fixtures/var-assessment.fixtures.json" with { type: "json" };
import { evaluatePush, evaluateVar, FORBIDDEN_SIGNATURE_KEY_PATTERN } from "../src/index.js";

type Json = Record<string, unknown>;

/** baseline 위에 케이스가 덮어쓴 값을 얹는다. 얕은 병합이면 충분하다 — 사실값은 한 겹이다. */
const mergeFacts = (baseline: Json, overrides: Json): Json => ({ ...baseline, ...overrides });

/** { value, observedAtSpeed } 형태에는 shotIds 기본값을 채운다. */
const hydrate = (facts: Json): Json => {
  const hydrated: Json = {};
  for (const [key, value] of Object.entries(facts)) {
    hydrated[key] =
      value !== null && typeof value === "object" && "value" in (value as Json)
        ? { shotIds: [], ...(value as Json) }
        : value;
  }
  return hydrated;
};

/** expect에 적힌 필드만 비교한다. 적지 않은 필드는 이 케이스의 주장이 아니다. */
const expectSubset = (actual: Json, expected: Json, caseId: string) => {
  for (const [key, value] of Object.entries(expected)) {
    expect(actual[key], `${caseId}: ${key}`).toEqual(value);
  }
};

const pushResults = new Map<string, EvaluationResult>();

describe("push-decision 픽스처", () => {
  beforeAll(() => {
    for (const testCase of pushSuite.cases) {
      // 케이스가 판본을 덮어쓸 수 있게 둔다. 지금 push 스위트에는 그런 케이스가
      // 없어서 JSON 추론 타입에 이 필드가 아예 없다 — 러너가 할 수 있는 일을
      // 픽스처가 지금 무엇을 담고 있는지에 매이게 하지 않는다.
      const ruleVersion =
        (testCase.given as { ruleVersion?: string }).ruleVersion ?? pushSuite.defaults.ruleVersion;
      const rules = loadRuleSet(ruleVersion);
      expect(rules, `${testCase.id}: 알 수 없는 판본 ${ruleVersion}`).not.toBeNull();
      const facts = hydrate(
        mergeFacts(pushSuite.baseline as Json, testCase.given.facts as Json),
      ) as unknown as PushFacts;
      pushResults.set(testCase.id, evaluatePush(facts, rules!));
    }
  });

  it.each(pushSuite.cases.map((testCase) => [testCase.id, testCase] as const))(
    "%s",
    (id, testCase) => {
      expectSubset(pushResults.get(id)! as unknown as Json, testCase.expect as Json, id);
    },
  );

  it("push-09 — 모든 결과에 조항 인용이 최소 하나 붙는다", () => {
    expect(pushResults.size).toBe(pushSuite.cases.length);
    for (const [id, result] of pushResults) {
      expect(result.citations.length, `${id}에 인용이 없다`).toBeGreaterThanOrEqual(1);
    }
  });

  it("push-10 — 사실값이 막혀도 accounts가 비지 않고 막힌 사실이 보고된다", () => {
    const invariant = pushSuite.invariants.find((entry) => entry.id === "push-10")!;
    const ids = invariant.appliesTo as string[];
    expect(ids.length).toBe(invariant.expectedAppliesCount);

    for (const id of ids) {
      const result = pushResults.get(id)!;
      expect(result.blockedFrom.length, `${id}: 막힌 사실이 없다`).toBeGreaterThan(0);
      expect(result.accounts.length, `${id}: accounts가 비었다`).toBeGreaterThan(0);

      const declared = new Set(
        result.accounts.flatMap((account) => account.requires.map((entry) => entry.fact)),
      );
      for (const blocked of result.blockedFrom) {
        expect(declared, `${id}: ${blocked.fact}가 accounts에 없다`).toContain(blocked.fact);
      }
    }

    // 막히지 않은 케이스가 목록에 섞이면 검사가 무의미해진다
    for (const testCase of pushSuite.cases) {
      if (ids.includes(testCase.id)) continue;
      expect(
        pushResults.get(testCase.id)!.blockedFrom.length,
        `${testCase.id}는 막히지 않아야 하는데 막혔다`,
      ).toBe(0);
    }
  });
});

type VarRun =
  | { ok: true; assessment: VarAssessment; signature: string; signatureInput: string }
  | { ok: false; error: string };

const varResults = new Map<string, VarRun>();

const runVarCase = (testCase: (typeof varSuite.cases)[number]): VarRun => {
  const ruleVersion =
    (testCase.given as { ruleVersion?: string }).ruleVersion ?? varSuite.defaults.ruleVersion;
  const rules = loadRuleSet(ruleVersion);
  expect(rules, `${testCase.id}: 알 수 없는 판본 ${ruleVersion}`).not.toBeNull();
  const options = ((testCase.given as { competitionOptions?: unknown }).competitionOptions ??
    varSuite.defaults.competitionOptions) as CompetitionOptions;
  const facts = mergeFacts(
    varSuite.baseline as Json,
    testCase.given.facts as Json,
  ) as unknown as VarFacts;

  const outcome = evaluateVar(facts, rules!, options);
  return outcome.ok
    ? {
        ok: true,
        assessment: outcome.assessment,
        signature: outcome.factSignature,
        signatureInput: outcome.factSignatureInput,
      }
    : { ok: false, error: outcome.error };
};

describe("var-assessment 픽스처", () => {
  beforeAll(() => {
    for (const testCase of varSuite.cases) {
      varResults.set(testCase.id, runVarCase(testCase));
    }
  });

  it.each(varSuite.cases.map((testCase) => [testCase.id, testCase] as const))(
    "%s",
    (id, testCase) => {
      const run = varResults.get(id)!;
      const expected = testCase.expect as Json;

      if ("error" in expected) {
        expect(run.ok, `${id}: 오류를 기대했는데 결과가 나왔다`).toBe(false);
        expect((run as { error: string }).error).toBe(expected.error);
        return;
      }

      expect(run.ok, `${id}: 결과를 기대했는데 오류가 났다`).toBe(true);
      expectSubset((run as { assessment: VarAssessment }).assessment as unknown as Json, expected, id);
    },
  );

  it("var-19 — 네 게이트가 항상 별도 필드로 나온다", () => {
    const successful = [...varResults.entries()].filter(
      (entry): entry is [string, Extract<VarRun, { ok: true }>] => entry[1].ok,
    );
    expect(successful.length).toBe(varSuite.cases.length - 1); // var-21은 오류 케이스

    for (const [id, run] of successful) {
      const gates = run.assessment as unknown as Json;
      for (const field of ["category", "withinTimeWindow", "thresholdMet", "reviewProcedure"]) {
        expect(Object.hasOwn(gates, field), `${id}: ${field} 필드가 없다`).toBe(true);
        expect(gates[field], `${id}: ${field}가 비었다`).not.toBeUndefined();
      }
    }

    // 네 값이 하나로 접혔는지를 보는 두 증거
    const reviewableButNoIntervention = successful.some(
      ([, run]) => run.assessment.reviewable && run.assessment.intervention === "NO_INTERVENTION",
    );
    expect(reviewableButNoIntervention, "검토 대상이면서 개입하지 않은 결과가 하나도 없다").toBe(
      true,
    );

    const outsideCategoryButThresholdMet = successful.some(
      ([, run]) => !run.assessment.reviewable && run.assessment.thresholdMet === "MET",
    );
    expect(outsideCategoryButThresholdMet, "범주 밖이면서 문턱을 넘은 결과가 하나도 없다").toBe(
      true,
    );
  });

  it("var-20 — fact_signature가 결정적이고 경기·심판 식별자를 포함하지 않는다", () => {
    const bySignature = new Map<string, string>();

    for (const testCase of varSuite.cases) {
      const first = varResults.get(testCase.id)!;
      if (!first.ok) continue;

      expect(first.signature, `${testCase.id}: 서명 형식`).toMatch(/^[0-9a-f]{64}$/);

      const rerun = runVarCase(testCase);
      expect(rerun.ok).toBe(true);
      expect(
        (rerun as Extract<VarRun, { ok: true }>).signature,
        `${testCase.id}: 서명이 실행마다 다르다`,
      ).toBe(first.signature);

      for (const key of Object.keys(JSON.parse(first.signatureInput) as Json)) {
        expect(
          FORBIDDEN_SIGNATURE_KEY_PATTERN.test(key),
          `${testCase.id}: 서명에 식별자 키 ${key}가 들어 있다`,
        ).toBe(false);
      }

      const seen = bySignature.get(first.signature);
      if (seen !== undefined) {
        // 같은 서명은 같은 사실 조합에서만 나와야 한다
        expect(seen, `${testCase.id}: 다른 입력이 같은 서명을 냈다`).toBe(first.signatureInput);
      }
      bySignature.set(first.signature, first.signatureInput);
    }

    expect(bySignature.size).toBeGreaterThan(1);
  });
});
```

`var-12`와 `var-13`은 사실값이 같고 판본만 다르므로 **같은 서명을 갖는 것이 맞다.** 서명은 사실 조합을 가리키지 판본을 가리키지 않는다. 판본은 `applied_rule_version_id`가 따로 들고 있다 (README 8절). var-20의 충돌 검사가 서명이 아니라 서명 원문을 비교하는 이유다.

- [x] **Step 2: 27 케이스와 4 불변식이 전부 도는지 확인한다**

Run: `npx vitest run packages/rule-engine/test/fixtures.test.ts --reporter=verbose`
Expected: PASS — `push-decision 픽스처` 10건(케이스 8 + 불변식 2), `var-assessment 픽스처` 21건(케이스 19 + 불변식 2)

- [x] **Step 3: 픽스처가 실제로 읽히는지 한 번 확인한다**

`packages/rule-engine/fixtures/var-assessment.fixtures.json`의 `var-14` `competitionOptions`를 잠깐 `true`로 바꾸고 러너를 다시 돌린다.

Run: `npx vitest run packages/rule-engine/test/fixtures.test.ts`
Expected: FAIL — `var-14: reviewable` 불일치

확인했으면 `false`로 되돌리고 다시 PASS를 확인한다. 이 단계는 픽스처가 통과하는 이유가 "검사가 느슨해서"가 아님을 확인하는 것이다.

---

### Task 12: 두 개의 구조 검사기와 전체 확인

**Files:**
- Create: `scripts/check/no-edition-literal.mjs`
- Create: `scripts/check/enums.mjs`

**Interfaces:**
- Consumes: `packages/shared-types/src/vocabulary.ts`, `packages/rule-data/data/**/*.json`, `packages/rule-engine/src/**`, `packages/rule-engine/fixtures/*.json` — 전부 파일로 읽는다.
- Produces: 없음. `npm run check`가 이 둘을 포함한다.

테스트는 **엔진이 무엇을 답하는지**를 지키고 이 두 검사기는 **엔진이 무엇을 알고 있으면 안 되는지**를 지킨다. 픽스처를 아무리 늘려도 "코드가 판본을 알고 있다"는 사실 자체는 테스트로 잡히지 않는다 — 판본을 아는 코드도 지금 있는 두 판본에 대해서는 정답을 낸다. 세 번째 판본이 나와야 깨지고, 그때는 이미 늦다.

두 스크립트 모두 `node:fs`만 쓰고 빌드 산출물에 의존하지 않는다. `enums.mjs`는 `vocabulary.ts`를 **상대 경로로 직접** import한다 (Node 23의 기본 타입 스트리핑). 패키지 이름으로 부르면 워크스페이스 심링크와 `main` 해석까지 검사기의 전제가 되는데, 검사기는 빌드가 깨진 상태에서도 돌아야 한다.

- [x] **Step 1: `no-edition-literal.mjs`를 쓴다**

```js
#!/usr/bin/env node
/**
 * 룰 엔진 소스에 판본 문자열이 있으면 실패한다.
 *
 * 판본별 차이는 packages/rule-data의 JSON이 소유한다 (README 3절·11절).
 * 엔진이 판본을 알면 규정 개정이 코드 배포와 묶이고, 개정 시점에
 * 고쳐야 할 곳이 데이터 한 곳이 아니라 데이터와 분기 양쪽이 된다.
 */
import { readdirSync, readFileSync, statSync } from "node:fs";
import { join, relative } from "node:path";

const ROOT = new URL("../../", import.meta.url).pathname;
const TARGET = join(ROOT, "packages/rule-engine/src");
const EDITION = /\b20\d{2}\s*[-/]\s*\d{2}\b/g;

const walk = (dir) =>
  readdirSync(dir).flatMap((entry) => {
    const full = join(dir, entry);
    return statSync(full).isDirectory() ? walk(full) : [full];
  });

const violations = [];

for (const file of walk(TARGET).filter((f) => f.endsWith(".ts"))) {
  const lines = readFileSync(file, "utf8").split("\n");
  lines.forEach((line, index) => {
    for (const match of line.matchAll(EDITION)) {
      violations.push(`${relative(ROOT, file)}:${index + 1}  ${match[0]}  ${line.trim()}`);
    }
  });
}

if (violations.length > 0) {
  console.error("엔진 소스에 판본 문자열이 있습니다. 판본 차이는 rule-data가 소유합니다.\n");
  for (const violation of violations) console.error(`  ${violation}`);
  process.exit(1);
}

console.log(`판본 리터럴 없음 — ${relative(ROOT, TARGET)}`);
```

- [x] **Step 2: 검사기가 실제로 잡는지 확인한다**

Run: `node scripts/check/no-edition-literal.mjs`
Expected: `판본 리터럴 없음 — packages/rule-engine/src`

`packages/rule-engine/src/var/evaluate-var.ts` 아무 곳에나 `const edition = "2026-27";` 한 줄을 넣고 다시 돌린다.

Run: `node scripts/check/no-edition-literal.mjs`
Expected: 종료 코드 1, 해당 파일과 줄번호 출력

확인했으면 그 줄을 지운다.

- [x] **Step 3: `enums.mjs`를 쓴다**

```js
#!/usr/bin/env node
/**
 * 픽스처에 쓰인 값이 shared-types의 어휘에 있는지 검사한다.
 *
 * 픽스처는 JSON이고 러너가 이중 단언으로 넘기므로 tsc가 값을 보지 않는다.
 * 이 검사가 없으면 오타 하나가 "타입에 없는 값을 기대하는 케이스"로 남고,
 * 그 케이스는 통과하는 동안 아무것도 지키지 않는다 (README 13절).
 *
 * 판본 데이터도 같은 이유로 tsc 밖에 있다 — loadRuleSet이 `as RuleSetFile`로
 * 받으므로 값은 아무도 보지 않는다. 그래서 필드별 제약을 따로 검사한다.
 */
import { readdirSync, readFileSync } from "node:fs";
import { join } from "node:path";
import * as vocabulary from "../../packages/shared-types/src/vocabulary.ts";

const ROOT = new URL("../../", import.meta.url).pathname;
const FIXTURES = join(ROOT, "packages/rule-engine/fixtures");
const RULE_DATA = join(ROOT, "packages/rule-data/data");

/** 어휘 = vocabulary.ts가 내보내는 모든 문자열 배열의 합집합 */
const allowed = new Set();
for (const value of Object.values(vocabulary)) {
  if (Array.isArray(value) && value.every((entry) => typeof entry === "string")) {
    for (const entry of value) allowed.add(entry);
  }
}

/** data/ 아래는 권한별 디렉터리로 나뉘므로 재귀로 훑는다. */
const jsonFiles = (dir) =>
  readdirSync(dir, { withFileTypes: true }).flatMap((entry) => {
    const full = join(dir, entry.name);
    if (entry.isDirectory()) return jsonFiles(full);
    return entry.name.endsWith(".json") ? [full] : [];
  });

const ruleDataFiles = jsonFiles(RULE_DATA);
const knownVersions = new Set(
  ruleDataFiles.map((file) => JSON.parse(readFileSync(file, "utf8")).versionId),
);

const problems = [];

/**
 * 판본 데이터의 필드별 어휘 제약.
 *
 * 합집합 검사로는 잡히지 않는 것을 잡는다. DOGSO는 SEND_OFF_CATEGORIES에
 * 있으므로 합집합에는 들어 있지만 창을 다시 여는 사유는 아니다. 엔진은 이
 * 목록의 값을 변환표 없이 그대로 windowException으로 쓰므로, 목록이
 * VAR_WINDOW_EXCEPTIONS를 벗어나면 데이터가 엔진에 표현 불가능한 값을
 * 요구하게 된다. 지금 데이터가 맞는 건 우연이지 검사된 결과가 아니었다.
 */
const RULE_DATA_FIELDS = [
  { path: ["timeWindowExceptions", "sendOffCategories"], vocabulary: "VAR_WINDOW_EXCEPTIONS" },
];

for (const file of ruleDataFiles) {
  const data = JSON.parse(readFileSync(file, "utf8"));
  for (const { path, vocabulary: name } of RULE_DATA_FIELDS) {
    const values = path.reduce((node, key) => node?.[key], data);
    if (!Array.isArray(values)) continue; // 없는 필드는 스키마의 일이다
    const permitted = new Set(vocabulary[name]);
    for (const value of values) {
      if (!permitted.has(value)) {
        problems.push(`${data.versionId}:${path.join(".")}  ${name}에 없는 값 "${value}"`);
      }
    }
  }
}

/** 사실값과 기대값 안의 문자열만 본다. id·note·concern·gate는 사람이 읽는 메모다. */
const collect = (node, path, into) => {
  if (typeof node === "string") {
    into.push([path, node]);
    return;
  }
  if (Array.isArray(node)) {
    node.forEach((entry, index) => collect(entry, `${path}[${index}]`, into));
    return;
  }
  if (node !== null && typeof node === "object") {
    for (const [key, value] of Object.entries(node)) {
      if (key === "shotIds") continue; // 자유 문자열 — 어휘가 아니다
      collect(value, `${path}.${key}`, into);
    }
  }
};

for (const file of readdirSync(FIXTURES).filter((f) => f.endsWith(".json"))) {
  const suite = JSON.parse(readFileSync(join(FIXTURES, file), "utf8"));
  const found = [];

  collect(suite.baseline, `${file}:baseline`, found);
  for (const testCase of suite.cases) {
    collect(testCase.given.facts, `${file}:${testCase.id}.given.facts`, found);
    collect(testCase.expect, `${file}:${testCase.id}.expect`, found);

    const version = testCase.given.ruleVersion ?? suite.defaults?.ruleVersion;
    if (version !== undefined && !knownVersions.has(version)) {
      problems.push(`${file}:${testCase.id}  알 수 없는 판본 "${version}"`);
    }
  }

  for (const [path, value] of found) {
    if (!allowed.has(value)) problems.push(`${path}  어휘에 없는 값 "${value}"`);
  }
}

if (problems.length > 0) {
  console.error("픽스처가 타입보다 앞서 나갔습니다.\n");
  for (const problem of problems) console.error(`  ${problem}`);
  console.error("\n값을 먼저 vocabulary.ts에 추가하거나 픽스처의 오타를 고치세요.");
  process.exit(1);
}

console.log(
  `어휘 확인 — 알려진 값 ${allowed.size}개, 판본 ${knownVersions.size}개, ` +
    `판본 데이터 필드 ${RULE_DATA_FIELDS.length}개`,
);
```

**두 가지를 검사한다.** 사실값·기대값의 모든 문자열이 어휘에 있는지, 그리고 케이스가 가리키는 판본 파일이 실제로 있는지다. 후자가 없으면 `ifab-2026-2027` 같은 오타가 러너에서 "판본을 못 찾음"으로만 보이고 어디가 틀렸는지는 안 보인다.

`"none"`·`"clear"`·`"uncertain"` 같은 소문자 값도 어휘다 (Task 1의 `DISPLACEMENT_LEVELS`·`OBSERVED_SEVERITIES`). 대문자만 검사하면 이 값들의 오타가 그대로 통과한다.

- [x] **Step 4: 검사기가 실제로 잡는지 확인한다**

Run: `node scripts/check/enums.mjs`
Expected: `픽스처 어휘 확인 — 알려진 값 N개, 판본 2개`

`push-decision.fixtures.json`의 push-01에서 `"inconclusiveReason": "CAMERA_INSUFFICIENT"`를 `"CAMERA_INSUFFICENT"`로 잠깐 바꾼다.

Run: `node scripts/check/enums.mjs`
Expected: 종료 코드 1, `push-decision.fixtures.json:push-01.expect.inconclusiveReason  어휘에 없는 값 "CAMERA_INSUFFICENT"`

되돌린다.

- [x] **Step 5: 전체를 돌린다**

Run: `npm run check`
Expected: PASS — 타입 검사, 판본 리터럴 검사, 어휘 검사, 전체 테스트가 순서대로 통과

- [x] **Step 6: 1단계 완료 기준을 확인한다**

README 14절의 1단계 기준은 **"합성 픽스처 27건과 불변식 4건이 통과한다"**이다. 숫자를 눈으로 세지 말고 세어서 확인한다.

Run: `npx vitest run --reporter=verbose 2>&1 | grep -c "✓"`

케이스별 통과 개수를 직접 확인한다:

Run: `npx vitest run packages/rule-engine/test/fixtures.test.ts --reporter=verbose 2>&1 | grep -E "push-[0-9]+|var-[0-9]+" | wc -l`
Expected: `31` (케이스 27 + 불변식 4)

마지막으로 엔진이 rule-data를 import하지 않는지 확인한다. 이 방향이 뒤집히면 판본 데이터가 엔진에 딸려 들어가고 Task 12의 첫 검사기가 무의미해진다.

Run: `grep -rn "rule-data" packages/rule-engine/src/ || echo "의존 없음"`
Expected: `의존 없음`

---
