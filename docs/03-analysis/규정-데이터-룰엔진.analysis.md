# 규정-데이터-룰엔진 — 갭 분석

- 기준 문서: `README.md` 14절 「1단계: 규정 데이터와 Rule Engine」 (이 프로젝트에는 `docs/02-design/`가 없다. README가 설계 SSOT다)
- 구현 SSOT: `docs/01-plan/features/규정-데이터-룰엔진.plan.md`
- 최초 분석: 2026-08-18 (`bkit:gap-detector`)
- 갱신: 2026-08-19 — 갭 1-a·5·7·8·9 반영

## 요약

**Match Rate 50% (4.0 / 8)**

README 14절 1단계는 여덟 항목을 나열한다. 그중 **코드 항목 셋은 전부 섰고, 규정 자료 항목 넷은 대부분 손대지 않았다.** 낮은 점수는 코드 품질이 아니라 이 분포에서 나온다.

```
npm run check → 종료 코드 0
  typecheck ✓   판본 리터럴 검사 ✓   어휘 검사 (값 89 · 판본 2 · 판본 데이터 필드 1) ✓
  테스트 88건 / 9파일 ✓   픽스처 러너 31 = 케이스 27 + 불변식 4
```

## 채점표 — README 14절 1단계 여덟 항목

| # | 항목 | 점수 | 근거 |
|---|------|------|------|
| 1 | 규정 폴더 구조 정리 · `metadata.json` 작성 | 0 | 폴더는 정리됨(`rules/` 12파일). `metadata.json`은 어느 판본에도 없음 (`README.md:231` 스스로 `미작성`) |
| 2 | KFA 한국어 대조판 확보 | 0.5 | `rules/kfa/2025-26/`에 PDF 1건 존재. `README.md:203`은 여전히 `미확보`로 적혀 있고 내용 대조는 하지 않음 |
| 3 | 대회요강 판본 지정 범위 재확인 (`INFERRED`→`CONFIRMED`) | 0 | `README.md:261-264` 네 행 모두 `INFERRED` 그대로 |
| 4 | `competition_rule_versions` 초기 데이터 확정 | 0 | 저장소 전체에서 이 식별자 등장 0회 |
| 5 | Law 12·VAR 프로토콜 수동 발췌 구조화 (2025/26 · 2026/27) | 1 | `packages/rule-data/data/ifab/{2025-26,2026-27}.json`. 조항 인용과 출처가 값마다 붙어 있음 |
| 6 | 밀기 규칙 정의 · 인터프리터 구현 | 1 | `build-accounts.ts` · `push-gates.ts` · `evaluate-push.ts` |
| 7 | 판정 → 근거 조항 경로 완성 | 1 | `rules.cite(conceptKey)` → `RuleCitation[]`. 인용이 비면 던진다 (`evaluate-var.ts`) |
| 8 | 합성 픽스처 작성 및 통과 | 0.5 | 27건 + 불변식 4건 전부 통과. 그러나 같은 절이 요구하는 "값 하나만 다른 쌍"이 밀기 쪽에서 5입력 중 2입력만 덮음 (갭 1) |

**보조 지표**

| 지표 | 값 |
|------|-----|
| 계획서 Task 완료 | 12 / 12 |
| 코드 산출물 (계획서가 선언한 파일) | 88% |
| 규정 자료 정비 | 13% |
| VAR 4게이트 분리 · 근거 규율 등 중점 4개 영역 | 100% |

## 갭 목록

### 닫힘

| 갭 | 내용 | 처리 |
|----|------|------|
| **5** (보통) | `evaluate-var.ts`가 `facts.sendOffCategory as VarWindowException`으로 캐스팅. `SendOffCategory` 8값 중 `DOGSO`·`SERIOUS_FOUL_PLAY`·`SECOND_CAUTION`·`ENTERING_VOR`은 대상 타입에 없다. tsc(`as RuleSetFile` 때문에)·`enums.mjs`(합집합만 봄)·픽스처(해당 케이스 없음) 셋 다 놓쳤다. **지금 데이터가 맞는 건 검사된 결과가 아니라 우연이었다** | 방어선 둘. (a) 엔진에 `isWindowException` 타입 가드 — 겹치지 않는 값이 오면 인용 없는 결과와 같은 부류의 데이터 오류로 보고 `throw`. (b) `enums.mjs`에 필드별 어휘 제약(`RULE_DATA_FIELDS`) 추가 — 합집합 검사로는 안 잡히는 것을 검사 시점에 잡는다. 둘 다 `DOGSO`를 주입해 발동 확인 |
| **7** (낮음) | `VAR_WINDOW_EXCEPTIONS`가 코드는 5값, `README.md:1204`는 3값. 확장 근거(IFAB VAR 1.10 / Law 5.3)는 타당하나 계획서의 "의도적 차이" 목록에 없었다 | 계획서 머리말을 "네 가지"→"다섯 가지"로 고치고 5번 항목 추가 |
| **8** (낮음) | 계획서가 "선후 관계가 나타나는 곳은 두 필드뿐"이라 적었으나 `procedureGate`가 검토 불가일 때 `NONE`을 돌려주므로 실제로는 세 필드. 동작은 옳고(`var-02`가 고정) 문장이 틀렸다 | 계획서 문장 수정 |
| **1-a** (높음의 일부) | `push-07` note가 "push-06과 displacement 하나만 다르다"고 했으나 둘 다 다름 | note를 baseline 기준으로 재지정 |
| **9** (낮음) | `push-08` note가 "push-02와 두 값이 다르다"고 했으나 셋이 다름 | note를 baseline 기준으로 재지정 |

### 열림

| 갭 | 심각도 | 내용 | 필요한 것 |
|----|--------|------|-----------|
| **1** | 높음 | 밀기 픽스처의 단일값 대조 쌍이 5입력 중 2입력만 덮는다 (`cameraSufficiency` 01↔02, `severity` 03↔04). `contactDetected`·`insidePenaltyArea`·`opponentDisplacement`는 쌍이 없다. 원인: VAR 스위트에는 baseline 케이스(`var-01`)가 있어 어떤 입력이든 1케이스 추가로 쌍이 서는데, 밀기에는 baseline 케이스가 없어 쌍마다 2케이스가 필요하다 | **결정 필요.** baseline 케이스 + 단일값 케이스를 넣으면 같은 절의 완료 기준 "합성 픽스처 27개"가 깨진다. 27을 유지할지, 늘리고 `README.md:2210`과 13절 표(2033-2036행)를 함께 고칠지 |
| **2** | 보통 | `rules/**/metadata.json` 부재. `README.md:237`은 파일별 체크섬 기록을 요구하는데 적을 곳이 없다. `.gitignore`의 허용 규칙이 지금은 아무것도 살리지 않는다 | 계획서에 Task 13 신설. 경로·크기·SHA-256은 디스크에서 산출 가능하나 **출처 URL과 취득일은 사용자만 아는 값** — 지어내지 않고 명시적 `null`로 둘지 확인 필요 |
| **3** | 보통 | `README.md:203`은 KFA 2025-26 `미확보`라 적었으나 `rules/kfa/2025-26/`에 PDF가 있다. 내용 대조는 안 됨 | PDF 내용 확인 후 README 상태 갱신 |
| **4** | 보통 | `README.md:261-264` 네 행 모두 `INFERRED`. `competition_rule_versions` 미착수. 코드 쪽 보류는 옳다(계획서가 확인되지 않은 매핑을 굳히기를 거부) — 갭은 **조사가 시작되지 않았다는 것** | 대회요강 원문에서 판본 지정 범위 확인 |
| **6** | 보통 | `.git.disabled/` 객체 트리에 규정 원문 블롭이 있는지 미확인. `README.md:2276`이 열린 항목 | **닫히기 전까지 저장소를 공개하지 않는다** |

### 갭으로 세지 않은 것

2단계 이후이거나 계획서가 이유를 적고 미룬 것: apps·workers·DB·업로드·UI, 미구축 6개 패키지, `layerConflicts: []`, 도달 불가한 `intervention: "CONFIRMED"`, README 8절 DB 열 중 사실 타입에 없는 것, 빈 `kleague`·`competition-options`·`schemas` 디렉터리, `resolveRuleVersion`, 디스크의 PDF(gitignore됨), 미채점 라벨 사례, 미사용 `.env.example` 변수.

## 검증 범위와 한계

- 최초 분석 에이전트는 Bash를 쓸 수 없어 `npm run check`를 실행하지 못했고, 계획서↔디스크 대조는 Task 8·12만 줄 단위로 했다. Task 1-7·9-11은 **미검증**이며 그래서 이상 없음을 주장하지 않는다.
- VAR 스위트의 단일값 쌍 16건은 `[미검증]`이다.
- `.git.disabled/` 내용은 `[추정]`이다 — 갭 6이 그 확인이다.
- 이번 갱신에서 닫은 갭 다섯 건은 코디네이터가 직접 실행해 확인했다 (검사기 발동 · 엔진 `throw` · `npm run check` 종료 0).

## 다음

Match Rate 50%는 **자료 항목 넷(1·2·3·4)이 전부 규정 원문 조사**이기 때문이다. 이 넷은 코드 수정으로 오르지 않는다. 갭 1만 코드로 닫을 수 있고, 그건 픽스처 개수 결정을 먼저 받아야 한다.
