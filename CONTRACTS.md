# 실행 모듈 계약

이 문서는 현재 코드의 책임과 연결 경계를 기록한다
코드 연결과 영상 인식 정확도 검증은 서로 다른 완료 조건이다

## 책임과 허용 의존성

| 모듈 | 입력과 출력 | 허용 의존성 | 부수효과와 한계 |
|---|---|---|---|
| 영상 검증 | 원본 파일 → 미디어 메타데이터 또는 검증 실패 | 미디어 검사 도구 | 파일 읽기와 미디어 검사 프로세스 실행이며 모델 추론 없음 |
| 영상 관측 | 원본·승인된 모델 출력 → 후보·측정·증거·비공개 산출물 | Worker 포트와 승인된 로컬 모델 | 파일 생성과 추론 가능이며 반칙·징계 결정 및 사실 승인 불가 |
| 구조 검증 | 외부 자료 → 구조 수용 여부 | 공통 자료형과 어휘 | 필드·참조·시간·크기 검사이며 실제 영상 내용의 정확성 보장 아님 |
| 객체 검증 | 저장소 참조 → 실제 크기·해시가 확인된 참조 | 객체 저장소 포트 | 외부 읽기 가능이며 DB 결과 저장과 별도 단계 |
| 사실 승인 | 출처·방법·사실·증거·규정 문맥 → 사용 가능 여부와 사유 | 서버 고정 검증 정보와 순수 규칙 | 구조나 모델 점수만으로 승인하지 않음 |
| 규정 평가 | 승인된 사실·규정 판본 → 질문별 결론 | 공통 계약과 규정 자료 | DB·영상 모델·객체 저장소 호출 없음 |
| 결과 조정 | Worker 제출 → 검증·평가·저장 순서와 처리 결과 | 시계·해시·저장소 포트 및 위 순수 검사 | 단계 호출과 실패 처리 책임이며 SQL 트랜잭션을 직접 소유하지 않음 |
| 저장소 | 검증된 저장 요청 → 원자적 저장 결과 | SQL와 저장 계약 검증 | 잠금·임대·판본·만료 재검사 및 이력 보존 |
| 조회 조립 | 세션·분석 식별자 → 내부 조회 모델 | 저장소와 규정 조회 기능 | 현행 예외로 후보 필터와 VAR 범주 평가를 조회 시 수행 |
| 공개 정책 | 내부 조회 모델 → 허용된 완료 결과 | 조회 모델과 순수 공개 변환 | 원시 관측·승인 진단·미완료 질문을 공개 판정으로 변환하지 않음 |

라우트와 실행 스크립트는 입력 변환과 의존성 조립을 담당한다
공통 자료형과 순수 규정 엔진에서 어댑터나 프레임워크를 역으로 참조하지 않는다

현재 `status-store`는 후보 필터와 VAR 적용 범주를 조회 시 계산한다
이것은 현행 조회 조립의 의존성 예외이며 이번 변경에서 제거하지 않는다
자동 밀기 결과는 재평가하지 않고 저장된 평가와 현재 근거·판본의 유효성을 다시 대조한다

## 버전과 운영 연결 상태

| 계약 | 현재 버전 | 운영 경계 |
|---|---|---|
| Worker 통신 | `video-observations-v5` | 서버와 Worker 헤더가 일치해야 작업 API 사용 가능 |
| 시각 인식 요약 | `perception-run-v1` | 기존 저장·조회 호환 |
| 시각·음향 인식 요약 | `perception-run-v2` | 기본 관측 경로이며 음향은 비공개 자료 |
| 일반 상호작용 관측 | `interaction-observation-v1` | 비공개 압축 산출물에 보존하며 독립 사건 DB 수신 계약 아님 |
| 기존 자동 밀기 평가 | `automatic-review-v1` | 후보별 차단·완료 저장 경로가 있으며 운영 사실 생산자 목록은 비어 있음 |
| 유형별 사건 관측 | `incident-record-v1` | 구조 검증과 출처 연결 계약이며 운영 Worker 제출 형식으로 활성화되지 않음 |
| 유형별 사건 평가 | `incident-evaluation-v1` | 독립 잡기 평가기 지원이며 기존 자동 밀기 저장 계약과 별개 |
| 잡기 평가 방법 | `holding-criteria-v2` | 승인 관측의 모순 보류와 사실별 비공개 진단 보존 |

평가기의 존재와 관측 방법의 승인은 독립적이다
새 평가기를 등록해도 운영 사실 생산자 목록이나 사실별 승인 조건을 자동 변경하지 않는다
현재 잡기 유형·방향·접촉·인과적 이동 방해를 실제 영상에서 검증한 생산자는 없다
일반 후보를 잡기 사건으로 강제 변환하거나 미확인값을 부정값으로 채우지 않는다

## 결과 제출 순서와 실패

1. 입력 식별자·임대·본문 구조 검사
2. 임대 토큰 해시와 사전 검사 시각 생성
3. 저장소에서 현재 권한 확인 및 필요 시 검증된 경기 문맥 연결
4. 원본 결합과 비공개 산출물·참조 증거의 실제 크기·해시 검사
5. 사실 사용 가능 여부와 차단 사유 계산
6. 파일 검사 이후 실제 시각으로 보존 기한 재확인
7. 기존 자동 밀기 평가 입력 구성과 평가
8. 저장소에서 잠금 후 재검사하고 결과 저장

예상한 입력·증거 불일치는 기존 공개 결과 판별 유니온으로 반환한다
실제 객체 저장소 읽기 장애는 기존 `INVALID_RESULT / STORAGE` 계약을 유지한다
사실 승인·평가·DB 저장의 예외를 객체 저장소 오류로 오인하지 않는다
동기 저장 예외와 비동기 저장 거절은 같은 단계에서 처리하며 서버 오류를 클라이언트 400으로 바꾸지 않는다
내부 실패 진단에는 단계와 작업 식별자·판본만 사용하며 본문·임대 토큰·객체 경로·원문 오류를 공개 응답에 넣지 않는다
진단 기록 실패가 원래 처리 오류를 덮어쓰지 않는다

## 트랜잭션 범위

사전 검사인 `JobStore.preflight`는 완전한 읽기 전용이 아니다
등록된 원본이고 경기·규정 문맥이 미연결인 경우 별도 트랜잭션에서 작업·분석·영상을 잠그고 권한을 재검사한 뒤 분석의 문맥 식별자와 상태 판본을 갱신한다
`automaticContext` 자체는 경기와 규정을 잠금 조회하며 새 경기·규정 행을 만들지 않는다
사전 문맥 연결은 뒤의 결과 저장과 독립적으로 커밋되므로 후속 객체 검사 실패로 되돌아가지 않는다
이 연결 시점을 변경하려면 구조 분리와 별개로 정책과 회귀 테스트를 검토해야 한다

`JobStore.result`는 결과 저장 트랜잭션을 소유한다
잠금 이후 현재 작업 임대·revision·원본·규정 문맥·보존 기한을 다시 검사한다
후보·증거·관측 요약·자동 평가·완료 상태·이벤트 저장과 최종 만료 검사 실패는 이 트랜잭션 안에서 함께 되돌린다
부수 함수로 분리하더라도 같은 트랜잭션을 전달하며 저장 단계별 독립 커밋을 만들지 않는다
객체 저장소 읽기와 미디어 업로드는 DB 트랜잭션 밖에 있고 DB 롤백이 객체 바이트를 삭제하지 않는다

## 공개 불변식

- 최종 공개 정책의 소유자는 `application/use-cases/status/report`이며 저장소 반환값을 그대로 공개하지 않음
- 저장 시 검증과 조회 시 재결합 및 공개 시 필드 제한을 모두 유지
- 과거 수동 이력은 기존 호환 계약으로 조회하며 새 자동 사실로 재사용하지 않음
- 표본 처리 완료와 VAR 범주 완료 및 전체 파울 판정 완료를 구분
- 미확인·미지원·모순을 파울 없음이나 카드 없음으로 변환하지 않음
- 사건별 진단인 `factDiagnostics`와 원시 관측은 공개 결과에서 제외
- 현재 `summary.truncated` 차단은 유지하며 개별 사건의 증거 완전성 계약 없이 완화하지 않음

## 후속 연결의 완료 조건

비공개 관측의 독립 수신·색인·조회와 유형별 사건 생산 및 승인·질문별 저장/공개는 후속 계약으로 연결한다
그 단계에서는 원본 해시·작업 판본·후보·사건·행위·사실·증거·승인·평가 관계를 함께 보존한다
필요한 DB 확장은 새 마이그레이션과 전송 버전으로 추가하고 기존 이력을 덮어쓰지 않는다
개발용 정답과 실제 영상의 사실별 성능 검증 없이 사실 생산자를 승인하지 않는다
코드 연결 시험의 성공을 접촉·반칙 인식 정확도나 실제 경기 판정 완료로 보고하지 않는다
