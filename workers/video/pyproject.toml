[project]
name = "replay-lab-video-worker"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "numpy>=1.26",
  "opencv-python>=4.8",
]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
